# Audit: feed-api

> Status legend: `PENDING` (not started) · `IN PROGRESS` · `DONE` · `FAILED` ·
> `N/A`. Every row traces to `intent.md` (goal / success criterion) or
> `contract.md` (interface / behavior guarantee). Live-AWS rows are **manual
> runbook steps**, never automated tests — the Phase 1 precedent.

## Requirements Checklist

> **Auditor pass 2026-09-01 (`sdd-auditor`).** Verdicts below are the
> auditor's own, from reading every implementation file and independently
> re-running the evidence (full suite; a scrubbed-credential re-run of the
> seven new test files; an in-process `Template.from_stack` synth *and* a real
> `uv run cdk synth --app "python infra/app.py" AiRadarFeedApi`, both with
> `docker info` failing; `uv run export_api_schema.py` re-run and diffed; the
> Dockerfile's `uv export`/`uv pip install --target` recipe executed for real
> outside the image). Scope note: roadmap Phase 6 (deploy, live curl,
> README/`CLAUDE.md`) was deliberately **not attempted** this session, so its
> rows are **DEFERRED**, not FAIL.

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | `GET /v1/cards` on an API Gateway **HTTP API** → Lambda → `Query` on `feed-by-score`, returning cards in relevance-desc/date-desc order | intent Goal 1 | PASS | `src/api/feed.py:62-75` builds exactly one `table.query(IndexName="feed-by-score", KeyConditionExpression=Key("gsi_pk").eq("CARD"), ScanIndexForward=False, Limit=…)`; no `Scan`/`put_item`/`update_item` token exists anywhere under `src/api/` or `src/contracts/` (auditor grep). Synth shows `AWS::ApiGatewayV2::Api` (`ProtocolType: HTTP`) → `AWS_PROXY` integration → `AWS::Lambda::Function`. Ordering asserted on real (moto) data by `test_query_feed_orders_by_relevance_desc_then_published_desc` and end-to-end through `handler()` by `test_handler_returns_200_with_feed_response_matching_seeded_order`. |
| R2 | `Card` promoted to a versioned, validated Pydantic schema (`CardOut`/`FeedResponse`) outside `src/curation/`, with `shared.cards.Card` unmodified | intent Goal 2 | PASS | `src/contracts/card.py` matches the contract's pinned source field-for-field. Auditor diffed `CardOut`'s fields against what `DynamoCardStore.upsert` actually SETs (`src/curation/dynamo.py:104-137`): identical content set + `card_id`/`created_at`/`updated_at`, minus `gsi_pk`/`gsi_sk`. `git status` shows **no** modification under `src/shared/` (see R12). |
| R3 | A committed JSON Schema artifact (`docs/api/feed-api.v1.schema.json`) that Spec 02 generates TypeScript types from | intent Goal 2 | PASS | Artifact committed, `$defs`-linked `CardOut` inside `FeedResponse`. Auditor re-ran `uv run export_api_schema.py` and diffed: **byte-identical**, so the artifact is reproducible, not hand-edited. |
| R4 | Tag filtering via `FilterExpression` on the same GSI query — no new index, no backfill | intent Goal 3 | PASS | `src/api/feed.py:70-71` adds `FilterExpression=Attr("tags").contains(tag)` to the *same* kwargs dict — one query, one index. No post-hoc filtering exists in `handler.py`. The synthesized IAM grants `Query` on one ARN ending `/index/feed-by-score`, which independently proves no second index is reachable. |
| R5 | Cursor pagination with an opaque token and a hard round-trip guarantee | intent Goal 4 | PASS | See C6/C7/C8. `test_cursor_round_trip_reproduces_unpaginated_sequence_{unfiltered,filtered}` walk `next_cursor` through the real `encode_cursor`/`decode_cursor` pair to exhaustion and assert sequence equality with a single unpaginated query, plus a no-duplicate-`card_id` assertion. |
| R6 | Least-privilege IAM: `dynamodb:Query` on the index ARN, scoped `logs` write, no managed policy, no wildcard, no write action | intent Goal 5 | PASS | Auditor dumped the synthesized policy document: exactly two statements — `FeedGsiQuery` (`Action: "dynamodb:Query"`, single literal index ARN) and `FeedApiLogsWrite` (`logs:CreateLogStream`/`PutLogEvents` on `Fn::GetAtt(LogGroup.Arn):*`). The role resource carries **only** `AssumeRolePolicyDocument` — no `ManagedPolicyArns` key at all. No `Resource: "*"` anywhere in the template. Notably the explicit `logs.LogGroup` + `log_group=` wiring also avoids CDK's `log_retention` custom resource, which would have added a second Lambda with a broader role. |
| R7 | CORS scoped to a configured origin list (never `*`), changeable in one place via `cdk deploy -c` | intent Goal 6 | PASS | Synth: `CorsConfiguration: {AllowHeaders: ["content-type"], AllowMethods: ["GET"], AllowOrigins: ["http://localhost:3000"], MaxAge: 3600}`. One module-level `DEFAULT_ALLOWED_ORIGINS` (`infra/lib/feed_api.py:22`); `FeedApiStack` reads `-c feed_api_allowed_origins` and the override is proven by `test_cors_origins_are_overridable_via_cdk_context`. Handler emits no `access-control-*` header. |
| R8 | An explicit, justified `pydantic` packaging decision that keeps `uv`/`uv.lock` the only dependency mechanism and keeps pytest Docker-free | intent Goal 7 | PASS (offline scope) | `[dependency-groups] api` present; `uv.lock` regenerated by `uv add --group api` (diff shows only the additive group). Auditor ran the Dockerfile's own recipe for real: `uv export --frozen --no-emit-project --only-group api` resolves to **pydantic + annotated-types + pydantic-core + typing-extensions + typing-inspection and nothing else** — no `boto3`, no `langgraph`/`bedrock-agentcore`/`tavily-python`/`feedparser`/`rich` — and `uv pip install --no-cache --target <dir> -r <that file>` succeeds **outside any virtualenv** (the "No virtual environment found" failure mode does not occur; uv discovers a system interpreter). No `requirements.txt` is committed. **2026-09-02 update (F2 closed):** the image was actually built (`docker build -f Dockerfile.feed_api --platform linux/arm64`), which surfaced a real gap the outside-the-container recipe check could not: `pydantic-settings` (required by `src/api/config.py`'s `_ApiSettings(BaseSettings)`, per contract.md's own pinned interface code) was missing from the `api` group — `--only-group api` only pulled `pydantic`, and the first `import api.handler` inside the built image raised `ModuleNotFoundError: No module named 'pydantic_settings'`. Fixed via `uv add --group api pydantic-settings`; group is now `["pydantic>=2.13.4", "pydantic-settings>=2.14.2"]`. Rebuilt image imports clean and a full `handler()` call against a fake table returns 200. See the Audit Log entry below. |
| R9 | CDK construct + stack in the Phase 1 pattern, wired into `infra/app.py`, referencing the existing table by name (creating none) | intent Goal 8 | PASS | `infra/lib/feed_api.py` (construct exposing `.http_api`/`.function`/`.log_group`/`.role`/`.allowed_origins`) → `infra/stacks/feed_api_stack.py` (4 `CfnOutput`s) → `infra/app.py:25,32` (one import + one line; the four existing stacks untouched). Synth resource types: ApiGatewayV2 Api/Integration/Route/Stage, IAM Role/Policy, Lambda Function/Permission, Logs LogGroup — **no** DynamoDB resource of any kind. |
| R10 | Server-side `limit` bounds (default 20, max 100) enforced, not merely documented | intent Success Criteria | PASS | `src/api/handler.py:34-52` (`_parse_limit`): absent → `DEFAULT_PAGE_SIZE`; `int()` failure or outside `[1, MAX_PAGE_SIZE]` → 400 `invalid_limit`, returned **before** `card_table()` is ever called. Proven no-AWS-call by the `_PoisonedTable` double across `"0"`, `"101"`, `"abc"`, `"-1"`, `"1.5"`, `""`. Never clamped. |
| R11 | Public-endpoint **compute** cost guards: stage throttling + reserved concurrency | intent Constraints / AD-7 | PASS | Synth: `AWS::ApiGatewayV2::Stage` `$default` with `DefaultRouteSettings {ThrottlingRateLimit: 20, ThrottlingBurstLimit: 40}`, `AutoDeploy: true`; function `ReservedConcurrentExecutions: 5`. The request-count residual is documented, not solved — as the contract says it is. No new alarm/metric was added (correct: `AiRadarBudget` is the backstop). **2026-09-02 addendum:** a deploy-time-only, opt-in context override (`-c feed_api_reserved_concurrency=none`/`=<n>`) now exists to bridge a real account-quota-provisioning gap (see the Audit Log). It is **off by default** — no context override → still exactly 5, asserted unchanged by `test_function_has_reserved_concurrency_arm64_and_timeout` — so this requirement's audited behavior is unaffected. |
| R12 | Zero changes under `src/curation/`, `src/shared/`, `runtime_app.py`, `Dockerfile`, and the four existing infra construct/stack pairs | intent Success Criteria | PASS | Auditor ran `git status --porcelain` + `git diff --stat`: the **only** modified tracked files are `.env.example` (+5), `infra/app.py` (+2), `pyproject.toml` (+3), `tests/conftest.py` (+84, additive fixtures only), `uv.lock` (+4). Nothing under `src/curation/`, `src/shared/`, `runtime_app.py`, `Dockerfile`, or `infra/lib|stacks/{card_store,agent_runtime,curation_schedule,cost_budget}*` is touched. |
| R13 | `uv run pytest tests/` green and 100% offline — no AWS, no network, no Docker daemon | intent Success Criteria | PASS | Auditor ran `uv run pytest tests/ -q` → **344 passed, 0 failed** (4.26s) with `docker info` returning non-zero. Re-ran the seven new files under scrubbed credentials (`AWS_CONFIG_FILE=/dev/null`, `AWS_SHARED_CREDENTIALS_FILE=/dev/null`, dummy keys, `AWS_EC2_METADATA_DISABLED=true`) → **83 passed**. `uv run cdk synth AiRadarFeedApi` also succeeded with no Docker daemon, computing the image-asset digest without building it — the AD-1 claim is independently confirmed, not merely asserted. |
| R14 | **Live**: deployed and curl-verified against the real `ai-radar-cards` table (page 1, cursor page 2, tag filter, 400s, CORS header presence/absence) | intent Goal 9 | **PASS — core read-path contract fully live-verified** | **2026-09-02/03, human-run, real evidence, in two passes.** `cdk deploy AiRadarFeedApi -c feed_api_reserved_concurrency=none --require-approval never` → `CREATE_COMPLETE`, real `FeedApiUrl`. Every sub-case named in this requirement's own wording is now confirmed against the live URL and the real `ai-radar-cards` table: page 1 real cards (M3), a genuine cursor round trip with zero overlap between pages (M4: `[983102b3ea3b4340, bc6639b3f24621f6]` then `[ae0bb8510d8a9c3c, d0601037cbc8406a]`), a real tag filter with every returned card verified to actually carry the tag (M5: `?tag=security` → 3/3 cards genuinely tagged `security`), all three documented 400 paths (M6: `limit=0`, `limit=101`, `cursor=garbage`), and both CORS halves (M7: allowed origin gets the header, a foreign origin `https://evil.example.com` gets **no** `access-control-*` header at all). **AD-6 is resolved** (M8): index-only `dynamodb:Query` is sufficient, no `AccessDeniedException`, no fallback needed. **Only non-blocking manual items remain open:** a pre-redeploy `cdk diff` sanity check on the other four stacks (M1), a cost/budget check (M9), and teardown (M10) — none of these gate anything today. **Still load-bearing, unchanged:** the deployed Lambda currently runs WITHOUT its audited `reserved_concurrent_executions=5` (see the 2026-09-03 Audit Log entries) — this is a temporary bridge state pending an AWS quota increase, not the final audited configuration; see M2 and R11/C20. |
| R15 | README runbook (deploy, verify, cost-while-up, teardown) + `CLAUDE.md` pointer updated | roadmap Phase 6 | DEFERRED | tasks.md 6.6/6.7 `[!]`, with the correct justification (the runbook should record what was really deployed and curled, not a hypothetical). Documentarian's step after a real deploy. |
| R16 | No domain layer, no extra endpoints, no auth, no write path, no metrics/EMF added | intent Non-Goals | PASS | Auditor checked each Non-Goal against the code: five flat modules under `src/api/` + one under `src/contracts/` (no aggregates/repositories/services); exactly **one** `AWS::ApiGatewayV2::Route` (`GET /v1/cards`, `AuthorizationType: NONE`, no authorizer resource, no `$default` route); zero write/`Scan` actions in IAM and zero write calls in code; no EMF/`put_metric`/alarm/WAF/CloudFront/custom-domain resource; `embedding` excluded from `ProjectionExpression` and dropped by `extra="ignore"`; no Bedrock import or permission anywhere. |

## Contract Compliance

> Each row was checked against the **implementation text**, not merely against
> a green test. "Verified by" names both the code site the auditor read and
> the test that exercises it.

| ID | Contract item | Status | Verified by |
|---|---|---|---|
| C1 | G1 — one `Query(IndexName="feed-by-score", Key("gsi_pk").eq("CARD"), ScanIndexForward=False)` per request; ordering correct; no `Scan` ever | PASS | Read `src/api/feed.py:62-75`: a single `kwargs` dict, one `table.query(**kwargs)`, no loop and no second call anywhere in the module. `handler()` calls `query_feed` exactly once. Grep for `Scan`/`scan` under `src/api/`+`src/contracts/` returns only `ScanIndexForward`. IAM cannot perform `Scan` (C10). Tests T1 (ordering), T31 (`_CountingQueryTable` proves `query_call_count == 1`), T14 (end-to-end order through the handler). |
| C2 | G2 — `?tag=X` applies `Attr("tags").contains(X)` as a `FilterExpression` on that same query; every returned card contains `X` | PASS | `src/api/feed.py:70-71` — the filter is added to the same kwargs, guarded by truthiness so `""`/`None` add nothing. No filtering logic exists in `handler.py` (read in full). T2 asserts membership and the absence of a non-matching card. |
| C3 | G3 — `len(cards) <= limit`; default 20; `[1,100]` enforced server-side | PASS | `Limit=limit` is always passed (never omitted), so DynamoDB itself bounds the page; per-item skips can only shorten it. `handler._parse_limit` rejects out-of-range/non-integer with 400 before any AWS call. T3 (query + handler default of 20), T9 (all five documented bad values + `""`). |
| C4 | G4 — a short/empty page with a non-null `next_cursor` is legal and produced by the filter-after-`Limit` behavior | PASS | No "top up the page" logic exists in `feed.py` or `handler.py` — the short page is returned verbatim, which is what makes the guarantee true. T4 covers both the short (`test_filtered_page_can_be_short_with_a_live_cursor`) and the genuinely **empty**-with-live-cursor case against real moto behaviour. |
| C5 | G5 — `next_cursor` is base64url(JSON(LEK)), padding stripped, and is `null` iff DynamoDB returned no `LastEvaluatedKey` | PASS | `cursor.encode_cursor` = `json.dumps(sort_keys, separators=(",",":"))` → `urlsafe_b64encode` → `rstrip("=")`. `handler.py:93-97` is a literal `if page.last_evaluated_key is not None … else None` — the iff holds by construction; `FeedPage.last_evaluated_key` is `resp.get("LastEvaluatedKey")`. T5 (padding-free, decodable), T6 (`None` only on the true last page). |
| C6 | G6 — **round trip**: paginated concatenation == unpaginated query, filtered and unfiltered, no dup/no gap | PASS | T7's two tests walk pages **through the real token** (`encode_cursor` → `decode_cursor` → `ExclusiveStartKey`), with a runaway-loop guard, and assert `[card_id] == [card_id]` against a single unpaginated query, unfiltered (13 items, limit 4) and tag-filtered (5 of 13 match), plus a set-size no-duplicates check. Code side: the decoded key is passed straight through untouched (`feed.py:72-73`), so nothing can reorder or re-anchor it. |
| C7 | G7 — `decode_cursor` rejects bad base64/JSON/key-set/value-type/foreign `gsi_pk`; 400 with **no** AWS call | PASS | Read `src/api/cursor.py:39-62`: five sequential rejections (base64, JSON, non-object, `set(keys) != CURSOR_KEYS`, non-`str` value, `gsi_pk != FEED_GSI_PARTITION`), each raising `InvalidCursorError`; the function returns the dict only after all five. In `handler.py` the decode happens at line 84, **before** `card_table()` at line 90 — ordering re-read by the auditor, and enforced by T10's `_PoisonedTable` (raises `AssertionError` if `.query()` is touched). T8 covers all six rejection paths. |
| C8 | G8 — `decode_cursor(encode_cursor(k)) == k`, and `encode_cursor` is deterministic | PASS | `sort_keys=True` + compact separators makes the encoding a pure function of the key's *content*, not its insertion order. T5's `test_encode_cursor_is_deterministic_regardless_of_key_order` asserts exactly that; `test_decode_of_encode_returns_the_original_key` asserts the inverse. |
| C9 | G9 — an item failing `CardOut` validation is skipped + counted; the page still returns 200 | PASS | `feed.py:79-84` — per-item `try/except ValidationError` → `logger.exception` + `skipped += 1` + omit, inside the loop (mirrors `DynamoCardStore.upsert`'s house rule). `skipped` is surfaced in the **log record only** (`handler.py:113`), never in the body — the response shape stays locked for Spec 02. T11. |
| C10 | G10 — role has no write/Bedrock/Secrets actions, no `Resource: "*"`, no managed policy | PASS | Auditor dumped the synthesized `AWS::IAM::Policy` document in full (see R6). Two statements, `dynamodb:Query` + two `logs:` actions, both on literal/`GetAtt` ARNs. The `AWS::IAM::Role` resource has no `ManagedPolicyArns` property at all. T15, T16, T30. |
| C11 | G11 — stack synthesizes zero `AWS::DynamoDB::Table` / `GlobalTable` | PASS | Auditor enumerated the synthesized resource types: none is a DynamoDB resource. The table is reached only through a literal ARN string. T17. |
| C12 | G12 — `AllowOrigins` == configured list, no `*`; `AllowMethods == ["GET"]`; handler emits no CORS headers | PASS | Synth output inspected directly (see R7). `handler._response` sets exactly one header, `content-type`; grep confirms no `access-control` string anywhere under `src/api/`. T18 (both default and context-override), T12. |
| C13 | G13 — committed schema artifact equals `contracts.card.json_schema()` | PASS | T19 compares **parsed dicts** rather than raw bytes (documented in the test's own docstring). That is semantically equivalent to the guarantee's "byte-for-byte after canonical JSON dumping" — both comparisons are key-order-insensitive — and the auditor closed the remaining gap directly by re-running `uv run export_api_schema.py` and diffing: zero bytes changed. Wording mismatch recorded as **F4 (LOW)**. |
| C14 | G14 — plane separation: `src/api`+`src/contracts` import no `curation`; `curation`/`shared` import neither; `boto3` only in `src/api/dynamo.py` | PASS | Auditor read every import in all seven new modules: `api.*` imports only `api.*`, `contracts.card`, stdlib, `pydantic`/`pydantic-settings`, and (in `dynamo.py` alone) `boto3`. `contracts/card.py` imports only `pydantic`. `curation`/`shared` are unmodified (R12), so the reverse direction holds trivially and is still guarded by T20 for the future. `api/config.py` does not import `shared` (AD-3), confirmed by reading the file. **Note F3:** `dynamo.py` re-exports `Attr`/`Key` and `feed.py` imports them from there, so the confinement is of the boto3 *import*, not of boto3 condition objects — this matches contract.md's own pinned code for `feed.py` and is not a deviation, but it is weaker isolation than the test name alone implies. |
| C15 | G15 — all tests offline: `moto` + `Template.from_stack`, no credentials/network/Docker | PASS | Independently re-verified, not taken on trust: full suite green with `docker info` failing; the seven new files green again under `/dev/null` AWS config+credentials files, dummy keys and `AWS_EC2_METADATA_DISABLED=true`; `cdk synth` of this stack likewise succeeds daemon-less and still emits a content-addressed `ImageUri`. |
| C16 | Error contract — 400 `invalid_limit`, 400 `invalid_cursor`, 200 empty for no-match tag, 500 `internal_error` without leaking the exception text, handler never raises | PASS | Read `handler.py` end to end: the whole body is inside one `try`, the sole `except Exception` logs via `logger.exception` and returns the fixed `{"error":"internal_error","message":"internal error"}` — the exception object never reaches `_error`'s arguments, so leakage is structurally impossible, not merely untested. T9, T10, T13, T22 (T22 asserts the sentinel `secret-internal-detail-12345` is absent from the body). |
| C17 | `CardOut` field-for-field parity with the item `DynamoCardStore.upsert` actually writes; `gsi_pk`/`gsi_sk`/`embedding` never returned | PASS | Auditor diffed `CardOut`'s 12 fields against `upsert`'s `UpdateExpression` (`src/curation/dynamo.py:107-137`) — exact match on content fields + `created_at`/`updated_at`, plus the PK `card_id`. Defence in depth is doubled: `ProjectionExpression` never asks for `gsi_pk`/`gsi_sk`/`embedding`, and `extra="ignore"` drops them if they ever arrive. T23 (with a real `embedding` seeded), T28. |
| C18 | AD-1/AD-2 — image built by `uv` from `uv.lock` with `--only-group api`; no `pip`/`requirements.txt` in the repo; arm64; `CMD ["api.handler.handler"]`; `src/curation`/`src/shared` not copied | PASS (F2 closed 2026-09-02) | `Dockerfile.feed_api` matches contract.md's pinned text line-for-line. Auditor executed the export/install recipe for real (see R8): correct closure, no boto3, works without a venv. `git status` confirms no `requirements.txt` is committed (the one the build makes is written to `/tmp` and `rm`'d in the same layer). Construct passes `platform=Platform.LINUX_ARM64` + `Architecture.ARM_64`; synth shows `Architectures: ["arm64"]`, `PackageType: "Image"`. T24 (3 trimmed tests). **F2 now closed**: a real `docker build -f Dockerfile.feed_api --platform linux/arm64` succeeded (exit 0), and — after adding the `pydantic-settings` dependency the outside-the-container check couldn't have caught (see R8) — `python -c "import api.handler, contracts.card"` and a full `handler()` call against a fake table both succeeded **inside the built image**. `docker images` reports **843 MB** (layer-history cross-check ≈ 636 MB; base `public.ecr.aws/lambda/python:3.12` image is ≈ 590 MB of that either way — this spec's own additions are ≈ 44 MB: 36 MB `uv` binary + 7.65 MB pydantic/pydantic-settings closure + ~70 KB app code). Local test image (`ai-radar-feed-api-local-check`) was removed after verification; nothing was pushed to any registry. |
| C19 | AD-4 — `api.config.FEED_GSI_NAME`/`FEED_GSI_PARTITION` equal `curation.config`'s, and the construct's ARN targets `/index/feed-by-score` | PASS | `src/api/config.py:35-36` vs `src/curation/config.py:143-144` — both `"feed-by-score"`/`"CARD"`, asserted by T20's drift test (the Tavily-sentinel remedy). Third copy in `infra/lib/feed_api.py:33-35`, and the synthesized ARN literally ends `/index/feed-by-score` (T15 asserts the full ARN). |
| C20 | AD-7 — `DefaultRouteSettings` throttling (20/40) and `reserved_concurrent_executions=5` present | PASS | Synth output read directly: stage `DefaultRouteSettings {ThrottlingBurstLimit: 40, ThrottlingRateLimit: 20}`, function `ReservedConcurrentExecutions: 5`. The `CfnStage` escape hatch is applied to `default_stage.node.default_child` exactly as pinned. T25. **2026-09-02:** `FeedApi.__init__` gained `reserved_concurrent_executions: int \| None = DEFAULT_RESERVED_CONCURRENCY` (default unchanged) and `FeedApiStack` reads a new opt-in context key `feed_api_reserved_concurrency` (mirrors AD-6's `grant_base_table_query` one-line-flip). With no context override the property is still exactly 5 (T25, unchanged). A new test, `test_reserved_concurrency_context_override_omits_the_property_entirely`, proves `-c feed_api_reserved_concurrency=none` **omits** `ReservedConcurrentExecutions` from the synthesized resource entirely (the only CloudFormation-correct way to leave a function's concurrency unreserved) rather than setting it to some falsy value. AD-7's audited intent (5) is unchanged; this is a documented deploy-time environmental bridge, not a design revision — see contract.md's AD-7 addendum and the Audit Log. |
| C21 | Route surface is exactly `GET /v1/cards` (`AuthorizationType: NONE`, `PayloadFormatVersion: "2.0"`), no `$default` route | PASS | Exactly one `AWS::ApiGatewayV2::Route` in the template; integration `IntegrationType: AWS_PROXY`, `PayloadFormatVersion: "2.0"`; the `AWS::Lambda::Permission` `SourceArn` ends `/*/*/v1/cards`, so even the invoke grant is path-scoped. No `$default` route or `default_integration` anywhere in the construct. T26. |
| C22 | Log group is CDK-created with `ONE_MONTH` retention and the role's `logs` grant is scoped to it | PASS | `AWS::Logs::LogGroup` `"/aws/lambda/ai-radar-feed-api"`, `RetentionInDays: 30`, `DeletionPolicy: Delete`; the function references it via `LoggingConfig.LogGroup`, and the `FeedApiLogsWrite` resource is `GetAtt(<that group>.Arn):*`. No `LogRetention` custom resource was synthesized. T16, T27, T30. |

## Test Coverage

> Statuses were the test-writer's own verdicts (`WRITTEN` = test exists and
> fails for the correct RED-phase reason). Per the run-observability
> precedent, the auditor's verdicts now replace them: **2026-09-01,
> `sdd-auditor` — all rows `PASS`** on a green `uv run pytest tests/`
> (344 passed, 0 failed), with T21's caveat spelled out in its own row. The
> 2026-09-01 human trims (T24: 7→3, T29: 6→3) were reviewed and accepted as
> written; the auditor did **not** re-litigate them.

| ID | Test description | Status | Test file |
|---|---|---|---|
| T1 | GSI query returns cards ordered relevance-desc then date-desc against a seeded set | PASS | `tests/test_feed_query.py` |
| T2 | `tag` filter returns only cards containing the tag; a card without it is absent | PASS | `tests/test_feed_query.py` |
| T3 | `limit` bounds the page; default is 20 when absent | PASS | `tests/test_feed_query.py`, `tests/test_feed_api_handler.py` |
| T4 | A filtered page can be shorter than `limit` (even empty) while still returning a cursor | PASS | `tests/test_feed_query.py` |
| T5 | `decode_cursor(encode_cursor(k)) == k`; encoding is deterministic and padding-free | PASS | `tests/test_feed_cursor.py` |
| T6 | `next_cursor` is `None` exactly on the last page | PASS | `tests/test_feed_query.py` |
| T7 | **Round trip**: walking `next_cursor` over a multi-page seeded set reproduces the unpaginated sequence exactly — unfiltered *and* tag-filtered | PASS | `tests/test_feed_query.py` |
| T8 | Every cursor rejection path raises `InvalidCursorError` (bad base64, non-JSON, non-object, missing/extra key, non-str value, `gsi_pk != "CARD"`) | PASS | `tests/test_feed_cursor.py` |
| T9 | Handler returns 400 `invalid_limit` for `0`, `101`, `"abc"`, `"-1"`, `"1.5"`, and (2026-09-02, F6) the comma-joined-duplicate-param shape `"5,6"`; 20 items max by default. `limit=1`/`limit=100` boundaries accepted (200), see T33 | PASS | `tests/test_feed_api_handler.py` |
| T10 | Handler returns 400 `invalid_cursor` and makes **no** DynamoDB call (poisoned table double raises if touched) | PASS | `tests/test_feed_api_handler.py` |
| T11 | A malformed stored item is skipped, counted, and the page still returns 200 with the rest | PASS | `tests/test_feed_query.py` |
| T12 | Handler response headers contain `content-type: application/json` and **no** `access-control-*` header | PASS | `tests/test_feed_api_handler.py` |
| T13 | Empty/whitespace `tag` is treated as absent; a no-match tag yields 200 with `cards: []` | PASS | `tests/test_feed_api_handler.py` |
| T14 | 200 body parses with `FeedResponse.model_validate_json` and matches the seeded order end-to-end through the handler | PASS | `tests/test_feed_api_handler.py` |
| T15 | IAM: exactly one `FeedGsiQuery` statement, action set `{dynamodb:Query}`, resource is the index ARN only | PASS | `tests/test_infra_feed_api.py` |
| T16 | IAM: no `Resource: "*"` anywhere, no `ManagedPolicyArns` on the function role, no write/Bedrock/Secrets actions | PASS | `tests/test_infra_feed_api.py` |
| T17 | Stack creates zero DynamoDB tables/global tables | PASS | `tests/test_infra_feed_api.py` |
| T18 | `CorsConfiguration.AllowOrigins` == configured list, contains no `*`; `AllowMethods == ["GET"]`; a context override changes it | PASS | `tests/test_infra_feed_api.py` |
| T19 | `docs/api/feed-api.v1.schema.json` equals `contracts.card.json_schema()` (drift guard) | PASS | `tests/test_feed_api_contract.py` |
| T20 | Plane separation + literal drift: AST import check (`api`/`contracts` ⇎ `curation`), `boto3` only in `src/api/dynamo.py`, GSI literals equal across planes | PASS | `tests/test_feed_api_contract.py` |
| T21 | Suite-level: no test constructs a non-mocked AWS client; synth tests need no Docker (asserted by the fact they pass in CI without a daemon) | PASS (by auditor re-run, not by an automated guard) | all new test files — there is no test that *enforces* this; the auditor verified it by (a) reading every fixture/test for a non-`moto` client, (b) re-running the seven new files with `AWS_CONFIG_FILE=/dev/null`, `AWS_SHARED_CREDENTIALS_FILE=/dev/null`, dummy keys and `AWS_EC2_METADATA_DISABLED=true` → 83 passed, and (c) confirming `docker info` fails on this machine while both `pytest` and `cdk synth` succeed |
| T22 | A raising table produces 500 `internal_error` whose body does **not** contain the underlying exception text; the handler does not raise | PASS | `tests/test_feed_api_handler.py` |
| T23 | Returned `CardOut` has every stored content field, `relevance` is an `int`, and `gsi_pk`/`gsi_sk`/`embedding` are absent even when `embedding` is seeded | PASS | `tests/test_feed_query.py` |
| T24 | **[trimmed 2026-09-01, per human review]** `Dockerfile.feed_api` uses `uv export --only-group api --frozen` with no bare `pip install` slipped into a `RUN` line (one test, `test_dependencies_are_installed_via_uv_export_with_only_group_api` — folds in what was a separate "no bare pip" test); does not copy `src/curation`/`src/shared` (`test_copies_only_api_and_contracts_never_curation_or_shared`); `CMD` references `api.handler.handler` (`test_cmd_points_at_the_handler_entrypoint`). Dropped: a standalone file-existence test (redundant — every surviving test's `_instructions()` helper already asserts it), the arm64/base-image check, and the standalone "no requirements.txt copied" check — all three guard failure modes a `cdk deploy`/first-invoke would surface loudly on its own, unlike the three kept tests, which guard *silent* regressions (image bloat/non-reproducible builds, a Plane A boundary leak, a wrong invoke entrypoint) | PASS | `tests/test_feed_api_dockerfile.py` |
| T25 | Stage `DefaultRouteSettings` throttling (20/40) and function `ReservedConcurrentExecutions == 5`, `Architectures == ["arm64"]`, `Timeout == 10` | PASS | `tests/test_infra_feed_api.py` |
| T26 | Exactly one route, `RouteKey "GET /v1/cards"`, `AuthorizationType NONE`, integration `PayloadFormatVersion "2.0"`; no `$default` route | PASS | `tests/test_infra_feed_api.py` |
| T27 | Log group name/retention and the four `CfnOutput`s (`FeedApiUrl`, `FeedApiFunctionName`, `FeedApiLogGroupName`, `FeedApiAllowedOrigins`) | PASS | `tests/test_infra_feed_api.py` |
| T28 | **[test-writer, new row]** `CardOut`/`FeedResponse` unit-level Pydantic contract: `Decimal`→`int` coercion (incl. rejecting a non-integer `Decimal`), missing `tags`/`takeaways` default to `[]`, `type` accepts any string (not an `Enum`), `extra="ignore"` drops `gsi_pk`/`gsi_sk`/`embedding`, `next_cursor` defaults to `None`, JSON round trip, `json_schema()`'s own shape (`$defs.CardOut`) — independent of the committed artifact (T19 checks that separately) and of any DynamoDB query (T23 checks the same guarantee end-to-end) | PASS | `tests/test_contracts_card.py` |
| T29 | **[test-writer, new row; consolidated 2026-09-01 per human review]** `src/api/config.py`: one parametrized test covers `CARD_TABLE_NAME`/`FEED_API_DEFAULT_PAGE_SIZE`/`FEED_API_MAX_PAGE_SIZE` env-overridability (3 cases, `test_api_config_field_is_env_overridable`); one test locks all three documented defaults together (`test_api_config_defaults_when_nothing_is_set`); the fixed `FEED_GSI_NAME`/`FEED_GSI_PARTITION` constants are NOT movable by a same-named env var (`test_fixed_gsi_constants_are_not_overridable_by_a_same_named_env_var`, unchanged); AD-3 (`api.config` does not import `shared`, `test_api_config_does_not_import_shared_per_ad3`) | PASS | `tests/test_feed_api_contract.py` |
| T30 | **[test-writer, new row]** AD-6 fallback: `FeedApi(grant_base_table_query=True)` adds the base-table ARN to the *same* `FeedGsiQuery` statement, still `Query`-only; the `FeedApiLogsWrite` statement is scoped to the function's own log group, not `"*"` | PASS | `tests/test_infra_feed_api.py` |
| T31 | **[test-writer, new row]** `query_feed` issues exactly one `Query` call per invocation (no draining/retry loop) | PASS | `tests/test_feed_query.py` |
| T32 | **[test-writer, new row; closes auditor finding F1, 2026-09-02]** `CARD_SCHEMA_VERSION` drift guard: `infra.lib.feed_api.ROUTE_PATH` starts with `f"/{CARD_SCHEMA_VERSION}/"` and the committed artifact's filename equals `f"feed-api.{CARD_SCHEMA_VERSION}.schema.json"` — the same duplicated-cross-boundary-literal shape as T20's GSI-literal drift test, applied to AD-5's three independently-hardcoded version strings | PASS | `tests/test_feed_api_contract.py` |
| T33 | **[test-writer, new row; closes auditor finding F6, 2026-09-02]** Payload-2.0 comma-joined duplicate query params: `?tag=a&tag=b` arrives as the literal tag `"a,b"` -> 200 with `cards: []` (matches nothing, not an error); `?limit=5&limit=6` arrives as `"5,6"` -> 400 `invalid_limit` (added as one more case to T9's existing parametrize list, not a new function). Documented boundary values `limit=1` and `limit=100` (`MAX_PAGE_SIZE`) are both accepted (200, not 400) | PASS | `tests/test_feed_api_handler.py` |

## Manual / live verification (runbook steps, not automated)

> **2026-09-01 audit note (superseded in part below).** As of the 2026-09-01
> audit, all rows below were PENDING/DEFERRED: Phase 6 had deliberately not
> been attempted (offline-only executor scope), and nothing was claimed. M8
> (AD-6: does index-only `dynamodb:Query` actually suffice?) was flagged as
> the single highest-value unknown left in this spec.
>
> **2026-09-02/03 update, two passes.** The human ran a real `cdk deploy
> AiRadarFeedApi -c feed_api_reserved_concurrency=none --require-approval
> never` and two real curl passes. **M2-M8 are all now DONE with real
> evidence** (recorded below and in the two 2026-09-03 Audit Log entries) —
> M8's answer resolves the spec's single highest-value open unknown:
> index-only `dynamodb:Query` IS sufficient, no `AccessDeniedException`, the
> `grant_base_table_query` fallback was never needed; M4 (cursor round trip)
> and M5 (tag filter) were confirmed with real, parsed response data, not
> just status codes; M6 and M7 were each confirmed on every documented
> sub-case, including the two initially-skipped ones (`limit=101`, the
> foreign-origin CORS-absence check). **The entire core read-path contract is
> now live-verified.** M1, M9, M10 remain genuinely PENDING — not attempted,
> not claimed, and none of them block anything right now. **The live Lambda
> is currently deployed WITHOUT its audited `reserved_concurrent_executions=5`**
> (the `feed_api_reserved_concurrency=none` override is still active, pending
> AWS Support case 178836416700301) — this is a temporary bridge state, not
> the fully-audited final one; do not read M2's DONE status as "the final
> state is deployed."

| ID | Step | Status | Evidence to record |
|---|---|---|---|
| M1 | `cdk diff` on `AiRadarCardStore`, `AiRadarRuntimeRole`, `AiRadarSchedule`, `AiRadarBudget` is empty before deploying | PENDING | Not run/reported. The deploy proceeded directly; recommended before the next redeploy (the one restoring `reserved_concurrent_executions=5`). |
| M2 | `cdk deploy AiRadarFeedApi` succeeds (image asset builds locally) | **DONE** | `cdk deploy AiRadarFeedApi -c feed_api_reserved_concurrency=none --require-approval never` → `CREATE_COMPLETE`, 11/11 resources, ~99s. Outputs: `FeedApiUrl=https://fdcksuokyh.execute-api.us-east-1.amazonaws.com`, `FeedApiFunctionName=ai-radar-feed-api`, `FeedApiLogGroupName=/aws/lambda/ai-radar-feed-api`, `FeedApiAllowedOrigins=http://localhost:3000`. **Deployed with the `feed_api_reserved_concurrency=none` override active — `ReservedConcurrentExecutions` is currently absent from the live function, NOT the audited final state of 5.** |
| M3 | `curl "$FEED_API_URL/v1/cards?limit=2"` returns real cards from `ai-radar-cards` | **DONE** (as `limit=3`) | `GET /v1/cards?limit=3` → 200, real curated cards (first card: a real "AI agents rapidly exploit security vulnerabilities…" story from the curation pipeline, not a fixture). |
| M4 | Page 2 via `next_cursor` returns a disjoint set of `card_id`s | **DONE** | Page 1 `?limit=2` → `[983102b3ea3b4340, bc6639b3f24621f6]`, `next_cursor` present. Page 2 using that exact cursor (`?limit=2&cursor=...`) → `[ae0bb8510d8a9c3c, d0601037cbc8406a]`. Four distinct `card_id`s across both pages, zero overlap — real disjoint pagination against the live table. |
| M5 | `?tag=<real tag>` narrows the result set | **DONE** | `GET /v1/cards?tag=security&limit=5` → 3 cards; every one of them genuinely has `"security"` in its `tags` list, verified by parsing the actual response body (not just trusting the count). |
| M6 | `?limit=0`, `?limit=101`, `?cursor=garbage` each return 400 with the documented error code | **DONE** | `?limit=0` → 400 `invalid_limit` (one transient local network connect timeout on the first attempt, unrelated to the API — a clean retry immediately after returned in 0.3s); `?limit=101` → 400 `invalid_limit`; `?cursor=not-a-real-cursor` → 400 `invalid_cursor`. All three documented cases confirmed. |
| M7 | CORS: allowed `Origin` gets `access-control-allow-origin`; a foreign origin does not | **DONE** | `OPTIONS /v1/cards` preflight with `Origin: http://localhost:3000` (allowed) → 204, `access-control-allow-origin: http://localhost:3000` present. `GET /v1/cards?limit=1` with `Origin: https://evil.example.com` (non-allowed) → response has **no** `access-control-*` header at all — confirms CORS truly restricts to the configured origin list, not merely that it echoes an allowed one. Both halves confirmed. |
| M8 | AD-6 resolution: index-only `Query` worked, or the base-table fallback was needed | **DONE — RESOLVED** | **Index-only `dynamodb:Query` (no base-table ARN) is sufficient.** The live `?limit=3` curl returned real cards with no `AccessDeniedException`; `grant_base_table_query` was never needed and remains `False` (the construct's default) on the deployed stack. This closes the spec's single highest-value open unknown. |
| M9 | Cost check: no new `AiRadarBudget` threshold crossed by this phase | PENDING | Not checked in this pass. |
| M10 | `cdk destroy AiRadarFeedApi` (when torn down) removes the API/Lambda/role/log group and leaves the RETAINed table intact | PENDING | Not attempted — the stack is intentionally still up (live-fire verification target); teardown is a separate future step. |

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-01 | human (inline review) | `tests/test_feed_api_contract.py`'s six `src/api/config.py` env-override/default tests (T29) were three near-duplicate copies of the identical shape (set one env var, reload, assert one field) — busywork, not three pieces of distinct logic | LOW | Consolidated to 3 tests: one parametrized test (`test_api_config_field_is_env_overridable`, 3 cases) for the override shape, one combined defaults test, and the fixed-constants-not-overridable test kept unchanged (a real invariant, not a naming test). The GSI-literal-drift test (`test_gsi_literals_match_between_api_config_and_curation_config`) was reviewed and kept as-is — it is the same precedented drift-guard pattern as the Tavily sentinel (F10), not a tautology. |
| 2026-09-01 | human (inline review, unrouted comment first found by test-writer as a stray `SyntaxError`) | `tests/test_feed_api_dockerfile.py` (T24) had 7 tests; the reviewer's blanket "all these tests are useless" was itself over-broad (these are functional Dockerfile-instruction assertions, the same pattern the repo's own `tests/test_dockerfile.py` already established as legitimate — not a Docker-internals test), but 4 of the 7 did guard failure modes a `cdk deploy`/first-invoke would surface loudly on its own (wrong/missing base image, a bare file-existence check redundant with every other test's internal guard, a standalone "no requirements.txt copied" check narrower than the packaging test that subsumes it) | LOW | Trimmed to 3: kept `test_dependencies_are_installed_via_uv_export_with_only_group_api` (folded the dropped `test_no_bare_pip_install_only_uv_pip_install`'s check in as an additional assertion in the same test body — same underlying AD-1/AD-2 regression class), `test_copies_only_api_and_contracts_never_curation_or_shared`, `test_cmd_points_at_the_handler_entrypoint`. Dropped `test_dockerfile_feed_api_exists`, `test_base_image_is_arm64_lambda_python`, `test_no_requirements_txt_copied_from_the_repo`. Module docstring rewritten to state the selection criterion explicitly (silent-regression classes only) rather than implying blanket coverage. Same trim-for-redundancy reasoning as the T29 resolution above. |
| 2026-09-01 | sdd-auditor | **F1 — `CARD_SCHEMA_VERSION` is declared but never used or drift-tested.** AD-5 says versioning is expressed three ways "all in one place": the route, the constant, and the artifact filename. In the implementation those are three *independent* hardcoded strings — `src/contracts/card.py:20` (`"v1"`), `infra/lib/feed_api.py:35` (`ROUTE_PATH = "/v1/cards"`), `export_api_schema.py:22` (`"feed-api.v1.schema.json"`) — and a repo-wide grep finds **zero** readers of the constant and **zero** tests asserting the three agree. This is precisely the duplicated-cross-boundary-literal shape AD-4 (and finding F10's Tavily-sentinel remedy) says must carry a drift guard. A `v2` bump can silently update one or two of the three, publishing an artifact whose filename disagrees with the route. Not a behavioural defect today: all three currently read `v1`. | LOW | **Recommended, not blocking.** One assertion alongside T20, e.g. `ROUTE_PATH.startswith(f"/{CARD_SCHEMA_VERSION}/")` and `f".{CARD_SCHEMA_VERSION}.schema.json" in OUTPUT_PATH.name`. Alternatively have `export_api_schema.py` build the filename from the constant. Deferred to the human review gate. |
| 2026-09-01 | sdd-auditor | **F2 — `Dockerfile.feed_api` has never been built.** Task 3.6 is honestly `[!]` (no Docker daemon in the executor sandbox; `docker info` still fails on this machine, auditor-confirmed). The file matches contract.md's pinned text line-for-line and is covered by three offline instruction-level tests, but "the image builds and imports" is unproven. The auditor reduced the unknown by executing the recipe outside a container: `uv export --frozen --no-emit-project --only-group api` resolves to exactly pydantic + annotated-types + pydantic-core + typing-extensions + typing-inspection (no boto3, no Plane A deps), and `uv pip install --no-cache --target <dir> -r <that file>` succeeds **with no virtualenv active** — closing the most likely suspected failure mode ("No virtual environment found"; uv discovers a system interpreter instead). | LOW | Accepted residual, correctly scoped to Phase 6. The first `cdk deploy` is the verification, and a build failure there is loud and immediate. No code change recommended. |
| 2026-09-01 | sdd-auditor | **F3 — boto3 confinement (G14) is of the import, not of the boto3 objects.** `src/api/dynamo.py` re-exports `Attr`/`Key` (`__all__`, line 17) and `src/api/feed.py` imports them from there, so `feed.py` still constructs `boto3.dynamodb.conditions` objects while the AST test sees no `boto3` root import. This satisfies Guarantee 14's exact wording ("`boto3` appears in `src/api/` only in `dynamo.py`") and matches contract.md's own pinned code for `feed.py`, which shows `Attr("tags").contains(tag)` inside that module — so it is **not** a deviation. Recorded so no future reader mistakes T20 for stronger isolation than exists. | LOW | No action. Documented behaviour; the re-export is already commented in `dynamo.py:14-16`. |
| 2026-09-01 | sdd-auditor | **F4 — G13's "byte-for-byte" is tested as parsed-dict equality.** `test_committed_schema_artifact_matches_contracts_json_schema` compares `json.loads(artifact)` with `json_schema()` and documents why (the export script owns formatting). Semantically equivalent to the guarantee (both comparisons are key-order-insensitive). The auditor closed the residual directly: re-running `uv run export_api_schema.py` produced a **byte-identical** file. | LOW | No action; the guarantee's wording is stricter than necessary, not the test weaker than necessary. |
| 2026-09-01 | sdd-auditor | **F5 — `pyproject.toml` pins `api = ["pydantic>=2.13.4"]`, contract.md's snippet says `pydantic>=2.13`.** Tighter than specified and matches the version the contract actually probed (2.13.4). Recorded only so the file and the contract text do not look like an unexplained divergence at the review gate. | LOW | No action. |
| 2026-09-01 | sdd-auditor | **F6 — two payload-2.0 edge behaviours are neither documented in contract.md's parsing table nor covered by a test.** Duplicate query params are comma-joined by HTTP API 2.0 (a fact contract.md pins), so `?tag=a&tag=b` becomes the literal tag `"a,b"` → 200 with an empty page, and `?limit=5&limit=6` becomes `"5,6"` → 400 `invalid_limit`. Both follow correctly from the implemented rules; neither is stated. Separately, `?limit=` (empty value) → 400 while `?tag=` → treated as absent — an intentional asymmetry, but the table documents only the `tag` half. Boundary values `limit=1` and `limit=100` are also untested (only `0`/`101` are). | LOW | Recommended for Spec 02's benefit: add the two boundary cases to T9's parametrize list and one sentence to the parsing table about comma-joined duplicates. Not blocking. |
| 2026-09-01 | sdd-auditor | **F7 — the G6 round trip is proven at the `query_feed` + real-token level, not through `handler()` to exhaustion.** T7 walks pages using the genuine `encode_cursor`/`decode_cursor` pair (i.e. the whole client-facing pagination mechanism), and T10/`test_handler_accepts_a_cursor_produced_by_encode_cursor` cover the handler's plumbing with a two-page disjointness assertion. What is not asserted is a full walk-to-`null` *through* `handler()`. The uncovered surface is three lines of pass-through (`handler.py:80-97`). | LOW | Optional hardening only; the guarantee's substance is tested where the logic lives. |
| 2026-09-02 | human (approved) + test-writer | **F1 and F6 closed via new tests (both coverage gaps, not bugs — implementation already behaved correctly for both).** F1: added `test_card_schema_version_is_embedded_in_route_path_and_artifact_filename` (T32) alongside the existing GSI-literal drift test in `tests/test_feed_api_contract.py`, asserting `ROUTE_PATH.startswith(f"/{CARD_SCHEMA_VERSION}/")` and `ARTIFACT_PATH.name == f"feed-api.{CARD_SCHEMA_VERSION}.schema.json"` against the real current values (`CARD_SCHEMA_VERSION = "v1"`, `ROUTE_PATH = "/v1/cards"`, artifact `feed-api.v1.schema.json`) — one structural invariant, asserted once, not three separate tests. F6: added `test_handler_treats_comma_joined_duplicate_tag_param_as_a_literal_string` (event built with the already-comma-joined `"a,b"` string directly, per the real payload-2.0 shape — not simulating the join), added `"5,6"` to T9's existing `bad_limit` parametrize list (folded in, no new function), and added `test_handler_accepts_the_documented_limit_boundaries` (T33, parametrized over `limit=1`/`limit=100`, both asserting 200 not 400). | LOW → RESOLVED | All 5 new test cases (1 F1 + 4 F6) **passed on first run against the existing implementation** — confirmed both findings were coverage gaps only, no implementation change was needed or made. `uv run pytest tests/` → **349 passed, 0 failed** (was 344; +5). |
| 2026-09-02 | executor (human-confirmed Docker daemon now available) | **F2 closed: `Dockerfile.feed_api` built and smoke-tested for real; a genuine packaging bug found and fixed in the process.** Ran `docker build -f Dockerfile.feed_api --platform linux/arm64 -t ai-radar-feed-api-local-check .` from the repo root (build context = repo root, matching `DockerImageCode.from_image_asset`) — first attempt succeeded (exit 0). Initial smoke check (`docker run --entrypoint python3.12 … -c "import api.handler, contracts.card"`) FAILED with `ModuleNotFoundError: No module named 'pydantic_settings'`: `pyproject.toml`'s `[dependency-groups] api` listed only `pydantic`, but `src/api/config.py` — per contract.md's own pinned interface code — is a `pydantic-settings` module (`class _ApiSettings(BaseSettings)`), and `uv export --only-group api` correctly excludes `pydantic-settings` since it's only a *main*-dependency-group transitive today, not a member of `api`. This was invisible to every prior offline check (pytest runs inside `.venv`, where `pydantic-settings` is already present as a main dependency; the auditor's outside-the-container `uv export`/`uv pip install --target` recipe check reads the export file but was never combined with an actual import inside that installed closure). Fixed with `uv add --group api pydantic-settings` (now `api = ["pydantic>=2.13.4", "pydantic-settings>=2.14.2"]`); `uv.lock` regenerated, not hand-edited. Rebuilt the image (second `docker build`, exit 0; one transient `DeadlineExceeded` resolving the base-image manifest on a retry in between, resolved by simply re-running — not a code issue). Re-ran the smoke check: `import api.handler, contracts.card` succeeded, and a full `handler()` call against a fake in-memory table (no `moto`, no real AWS) returned `{"statusCode": 200, "body": "{\"cards\":[],\"next_cursor\":null}"}` as expected. `uv run pytest tests/` re-run after the fix: **349 passed, 0 failed** (unchanged pass count from before the fix — the offline suite could not have caught this gap, confirming it as a real blind spot the local build closed, not a regression). Image size: `docker images` reports **843 MB** total; `docker history` layer-sum cross-check ≈ 636 MB (the two disagree, likely a Docker-Desktop/containerd-image-store display quirk — recorded as observed, not reconciled further, since it doesn't change the qualitative AD-1 claim). Of that, the AWS base image accounts for ≈ 590 MB either way; this spec's own additions are ≈ 44 MB (36 MB `uv` binary, left in the final image since the Dockerfile never removes it after use — matches contract.md's pinned text exactly, so not a deviation, but worth noting as the single largest of this spec's own additions to image size; 7.65 MB pydantic+pydantic-settings closure; ~70 KB app code). No AWS calls, no registry push, no `cdk deploy` — build and smoke checks only, per the human's explicit instruction. The local test image (`ai-radar-feed-api-local-check`) was removed afterward (`docker rmi`) to avoid cluttering `docker images`. | LOW → RESOLVED (with a real bug fixed, not just a residual closed) | `pyproject.toml`/`uv.lock` updated (`uv add --group api pydantic-settings`); no other file changed. Task 3.6 in `tasks.md` changed from `[!]` to `[x]`. Deploy/live-verify (Phase 6) remains untouched and still blocked pending the human's deploy decision. |
| 2026-09-02 | executor (human-approved, real live-fire AWS quota block) | **Deploy-time environmental override for `reserved_concurrent_executions` — not a design change; AD-7's audited default (5) is unchanged.** The real `cdk deploy AiRadarFeedApi` first attempt against this AWS account failed: the account's Lambda "Concurrent executions" quota was **10**, not AWS's stated default of 1000, and `reserved_concurrent_executions=5` was rejected by AWS because reserving 5 would drop the account's *unreserved* concurrency below AWS's required floor of 10. A quota increase to 1001 was requested (AWS Support case `178836416700301`, pending, no ETA); self-service Service Quotas requests below the default value are rejected outright for this quota, so there was no smaller ask available. Added the exact same one-line-flip pattern AD-6 already established for `grant_base_table_query`: `FeedApi.__init__` gained `reserved_concurrent_executions: int \| None = DEFAULT_RESERVED_CONCURRENCY` (default unchanged), and only includes `reserved_concurrent_executions=...` in the `DockerImageFunction` kwargs when the value is not `None` (omitting the kwarg — not passing `None` to CDK — is the only CloudFormation-correct way to leave concurrency unreserved). `FeedApiStack` reads a new opt-in context key, `feed_api_reserved_concurrency`: no key → construct's own default (5, unchanged); `"none"` (case-insensitive) → `reserved_concurrent_executions=None` (omitted); a digit string → `int()`'d and passed through. Verified all three paths with real `cdk synth`: no override → `ReservedConcurrentExecutions: 5` in the template (unchanged, T25 still green); `-c feed_api_reserved_concurrency=none` → the `ReservedConcurrentExecutions` key is **absent** from the synthesized `AWS::Lambda::Function` resource entirely, not merely falsy; `-c feed_api_reserved_concurrency=7` → `ReservedConcurrentExecutions: 7`. Added one new test, `test_reserved_concurrency_context_override_omits_the_property_entirely` (`tests/test_infra_feed_api.py`), asserting the key's absence for the `none` case — a real behavior assertion (the property must not exist, not just be unequal to 5), not a tautology. `contract.md`'s AD-7 section gained a documented paragraph recording this as a live-discovered deploy-time environmental constraint. `uv run pytest tests/` → **350 passed, 0 failed** (was 349; +1, the new override test). No AWS deploy was run by the executor — `cdk synth` only, per instruction; the human will run the real `cdk deploy -c feed_api_reserved_concurrency=none` themselves. | LOW (documented environmental workaround, opt-in, off by default) | `infra/lib/feed_api.py`, `infra/stacks/feed_api_stack.py`, `tests/test_infra_feed_api.py`, `specs/feed-api/contract.md` (AD-7 addendum) updated. R11/C20 rows above updated to note the override exists and is off by default. No change to R6/C10 (IAM) — unrelated to this override. |
| 2026-09-03 | human (real deploy) + conductor (relayed results) | **Real `cdk deploy AiRadarFeedApi` + live curl verification — the spec's Goal 9 is substantively met; AD-6 is resolved; the deployed Lambda is in a temporary no-reservation bridge state.** Deploy: `cdk deploy AiRadarFeedApi -c feed_api_reserved_concurrency=none --require-approval never` → stack `CREATE_COMPLETE`, 11/11 resources, ~99s. Outputs: `FeedApiUrl=https://fdcksuokyh.execute-api.us-east-1.amazonaws.com`, `FeedApiFunctionName=ai-radar-feed-api`, `FeedApiLogGroupName=/aws/lambda/ai-radar-feed-api`, `FeedApiAllowedOrigins=http://localhost:3000`. Live curl, all against the real URL and the real `ai-radar-cards` table: (1) `GET /v1/cards?limit=3` → 200, real curated cards — first card a genuine "AI agents rapidly exploit security vulnerabilities…" story from the curation pipeline, not a fixture. **This resolves AD-6: index-only `dynamodb:Query` (no base-table ARN) is sufficient** — no `AccessDeniedException`; the `grant_base_table_query` fallback (implemented and offline-tested since 2026-09-01) was never exercised and remains at its default (`False`). (2) `GET /v1/cards?limit=0` → 400 `{"error":"invalid_limit",...}` (one transient local network connect-timeout on the first attempt, unrelated to the API — an immediate retry succeeded in 0.3s). (3) `GET /v1/cards?cursor=not-a-real-cursor` → 400 `{"error":"invalid_cursor",...}`. (4) `OPTIONS /v1/cards` preflight with `Origin: http://localhost:3000` → 204, `access-control-allow-origin: http://localhost:3000` present. **Not exercised, recorded honestly as still open:** cursor-page-2 disjointness (M4), `?tag=` narrowing (M5), `?limit=101` (part of M6), foreign-origin CORS absence (part of M7), `cdk diff` on the other four stacks (M1), a post-deploy `AiRadarBudget` cost check (M9), and teardown (M10). **Load-bearing caveat, not a residual to lose track of:** the deployed Lambda currently has **no `ReservedConcurrentExecutions`** — it was deployed with the `feed_api_reserved_concurrency=none` context override (see the 2026-09-02 Audit Log entry on this same override mechanism) because this account's Lambda concurrency quota (10) could not yet support the audited `reserved_concurrent_executions=5`. AWS Support case `178836416700301` (quota increase to 1001) is still `CASE_OPENED` as of this entry. **The currently-live stack is a deliberate, temporary bridge state — NOT the fully-audited final configuration.** The human will run a plain `cdk deploy AiRadarFeedApi` (no `-c` override) once the quota clears, restoring `reserved_concurrent_executions=5`; only then is AD-7's audited baseline actually live, not merely synthesizable. | — (informational; not a defect) | `specs/feed-api/audit.md` R14 (DEFERRED → PASS, core-goal-met framing with the partial-coverage caveat spelled out), the Manual/live-verification table (M2/M3/M8 → DONE, M6/M7 → PARTIAL, M1/M4/M5/M9/M10 unchanged PENDING), and `specs/feed-api/tasks.md` (Tasks 6.2, 6.5, 6.8 → `[x]`; Tasks 6.3/6.4 → `[~]` partial; Task 6.1 → `[ ]` not verified; Tasks 6.6/6.7 deliberately left `[!]` open per explicit instruction, pending the post-quota-clearance redeploy) updated to match. `README.md`/`CLAUDE.md` deliberately NOT touched this pass. |
| 2026-09-03 | human (real curl, second pass) + conductor (relayed results) | **Remaining live-curl gaps closed: cursor round trip, tag filter, `limit=101`, and foreign-origin CORS absence — all against the same deployed URL (`https://fdcksuokyh.execute-api.us-east-1.amazonaws.com`).** (1) M6 fully closed: `GET /v1/cards?limit=101` → 400 `invalid_limit` (the third and last documented bad-limit case; `0` and `cursor=garbage` were already confirmed in the prior pass). (2) M7 fully closed: `GET /v1/cards?limit=1` with `Origin: https://evil.example.com` (a non-allowed origin) → response has **no** `access-control-*` header at all — confirms CORS genuinely restricts to the configured origin list rather than merely echoing whatever origin is sent. (3) M4 closed: cursor round trip — page 1 `?limit=2` → cards `[983102b3ea3b4340, bc6639b3f24621f6]` with a `next_cursor`; page 2 using that exact cursor (`?limit=2&cursor=...`) → cards `[ae0bb8510d8a9c3c, d0601037cbc8406a]`. Four distinct `card_id`s across both pages, zero overlap — a real disjoint pagination result against the live table, not just an offline `moto` assertion. (4) M5 closed: `GET /v1/cards?tag=security&limit=5` → 3 cards, and every one of them was confirmed (by parsing the actual response body's `tags` list, not just trusting the returned count) to genuinely contain `"security"`. **The entire core read-path contract (M3-M8) is now live-verified end to end.** No code change was needed or made — this pass is evidence-gathering only, confirming behavior the offline test suite (T2, T7) already asserted holds against the real deployed system too. | — (informational; not a defect) | `specs/feed-api/audit.md`: R14 upgraded to "core read-path contract fully live-verified"; Manual/live-verification table rows M4, M5, M6, M7 → DONE (was PENDING/PARTIAL). `specs/feed-api/tasks.md`: Tasks 6.3 and 6.4 → `[x]` DONE (were `[~]` PARTIAL). Task 6.1 (`cdk diff` on the other four stacks) and Tasks 6.6/6.7 (README/`CLAUDE.md`) intentionally unchanged — still open, per explicit instruction, pending the post-quota-clearance redeploy. The deployed Lambda's no-reservation bridge state (no `ReservedConcurrentExecutions`, pending AWS Support case 178836416700301) is unchanged and still the honest caveat on every one of these DONE marks. |
| 2026-09-01 | sdd-auditor | **Positive finding worth keeping:** the explicit `logs.LogGroup` + `log_group=` wiring (rather than CDK's `log_retention=` prop) is what makes Guarantee 10 achievable at all — `log_retention` would have synthesized a `LogRetention` custom-resource Lambda with its own broader role and an extra `AWS::Lambda::Function`. The auditor confirmed the template contains exactly one Lambda function and no custom resource. Likewise the `AWS::Lambda::Permission` `SourceArn` is path-scoped (`/*/*/v1/cards`), so even the invoke grant cannot be reused for a future second route. | — | No action; recorded as evidence for the review gate. |

## Final Verdict

> **2026-09-03 addendum (not a re-audit — see the dated Audit Log entries and
> the R14/M-table updates above for the actual evidence).** The verdict below
> is preserved as written on 2026-09-01 for history. Two of its stated
> reasons for withholding full approval no longer hold as stated: Goal 9's
> core ("a real HTTPS request against the real table returning real cards")
> is now met, and AD-6 is now resolved (index-only `Query` is sufficient).
> What is still genuinely open: the cursor round trip and tag filter against
> real data (both explicitly named in R14), and — newly load-bearing — **the
> live Lambda is currently deployed WITHOUT its audited
> `reserved_concurrent_executions=5`** (temporary bridge state pending AWS
> Support case `178836416700301`). This spec is **still not "done"**, just
> for a different, narrower, and now-precisely-stated reason than the
> original verdict gives.

**Audit date**: 2026-09-01 · **Auditor**: `sdd-auditor` · **Scope**: roadmap
Phases 1-5 (offline implementation). Phase 6 (deploy, live curl,
README/`CLAUDE.md`) was deliberately **not attempted** and is audited as
DEFERRED, not FAIL.

**Status**: **APPROVED WITH RESERVATIONS** — approved for merge as an offline
implementation; **not** approved as "done", because the spec's own Goal 9
(really deploy it and really curl it) and its one genuinely open technical
question (AD-6) are still ahead. *(See the 2026-09-03 addendum above: this is
now partially superseded — Goal 9's core and AD-6 are resolved; a narrower,
still-real gap remains.)*

**Summary**: Every one of contract.md's 15 Behavior Guarantees was checked
against the implementation text, not just against a green suite, and all 15
hold — several of them structurally (the cursor is decoded before
`card_table()` is ever called; the 500 path cannot leak an exception message
because the exception object never reaches the response builder; `Limit` is
always passed so `len(cards) <= limit` cannot be violated). All 22 Contract
Compliance rows PASS, 14 of 16 Requirements PASS and the remaining two are the
deliberately-deferred Phase 6 rows. `uv run pytest tests/` is green at
**344 passed, 0 failed**, and the offline claim was re-verified independently
rather than taken on trust. The reservations are all LOW and none touches a
behaviour guarantee.

**Critical issues** (must fix before merge): **none.** No contract deviation,
no missing interface, no broken guarantee, no scope creep, no unspecified
dependency, and no edit to any file the spec forbids touching.

**Warnings** (should fix, not blocking):
- **F1 (LOW) — RESOLVED 2026-09-02.** `CARD_SCHEMA_VERSION` was dead: declared
  at `src/contracts/card.py:20` and referenced by nothing. AD-5's "versioning
  is expressed three ways, all in one place" was in practice three independent
  hardcoded strings (`"v1"`, `ROUTE_PATH = "/v1/cards"`,
  `"feed-api.v1.schema.json"`) with no drift guard — in a spec that
  *elsewhere* (AD-4) insists duplicated cross-boundary literals get one.
  Closed by T32 (`tests/test_feed_api_contract.py`), which passed against the
  existing implementation on first run — confirming this was a coverage gap,
  not a bug.
- **F6 (LOW) — RESOLVED 2026-09-02.** Comma-joined duplicate query params
  (`?tag=a&tag=b` → literal tag `"a,b"`; `?limit=5&limit=6` → 400) were
  undocumented and untested, as were the `limit=1`/`limit=100` boundaries.
  Closed by T33 plus one added case in T9 (`tests/test_feed_api_handler.py`),
  all of which passed against the existing implementation on first run.
- **F2 (LOW) — RESOLVED 2026-09-02.** `Dockerfile.feed_api` had never been
  built (Task 3.6 `[!]`, no daemon in the executor sandbox). Once a Docker
  daemon became available, a real `docker build -f Dockerfile.feed_api
  --platform linux/arm64` was run from the repo root and succeeded. The build
  itself surfaced a genuine gap the auditor's outside-the-container recipe
  check could not: the `api` dependency group was missing `pydantic-settings`
  (required by `src/api/config.py`'s `_ApiSettings(BaseSettings)`, per
  contract.md's own pinned interface code) — `import api.handler` inside the
  freshly-built image raised `ModuleNotFoundError: No module named
  'pydantic_settings'`. Fixed with `uv add --group api pydantic-settings`;
  rebuilt, and both `import api.handler, contracts.card` and a full
  `handler()` call against a fake table succeeded inside the image. Full
  `uv run pytest tests/` stayed green (349 passed) after the fix. See the
  2026-09-02 Audit Log entry.

**Recommendations** (nice to have):
- **F7 (LOW)** — optionally assert the cursor round trip end-to-end through
  `handler()` (walk to `next_cursor is null`), not only through `query_feed`
  plus the real token pair.
- Synth-test hardening the current file does not cover: `MemorySize == 512`,
  `Environment.CARD_TABLE_NAME == "ai-radar-cards"` on the function,
  `CorsConfiguration.AllowHeaders`/`MaxAge`, and that the function's `Role` is
  the hand-authored one. All are asserted-by-construction today; none is a
  guarantee.
- **F3/F4/F5 (LOW)** — no action needed; recorded so the review gate is not
  surprised by them.

**What remains genuinely unverified** (stated without rounding up):
- **2026-09-03 update (two passes) — R14 / M2-M8 all now resolved.** Real
  `cdk deploy AiRadarFeedApi` + two real curl passes happened (see both
  2026-09-03 Audit Log entries): `?limit=3` returned real cards (M2, M3); the
  cursor round trip returned four distinct, non-overlapping `card_id`s across
  two pages (M4); `?tag=security` returned 3 cards each genuinely carrying
  the tag (M5); all three documented 400 cases including `limit=101` (M6);
  and both CORS halves, including the foreign-origin-absence case (M7).
  **Still genuinely unverified, not blocking:** `cdk diff` on the other four
  stacks (M1), a cost/budget check (M9), and teardown (M10). The roadmap's
  own risk register flag ("pydantic rejects a real stored item, e.g. a
  legacy card missing `takeaways`") is *partially* addressed — several real
  items across multiple queries (limit=3, limit=2 x2, tag=security) were
  validated by `CardOut` without incident — but not exhaustively, since only
  a handful of an unknown total number of stored cards were observed.
- **2026-09-03 update — M8 / AD-6 is RESOLVED, no longer unknown.** The live
  curl proved index-only `dynamodb:Query` (scoped to the `feed-by-score`
  index ARN, no base-table ARN) is sufficient — no `AccessDeniedException`.
  The `grant_base_table_query=True` fallback remains implemented and tested
  (T30) but was never needed and is not active on the deployed stack.
- **R15 — no README/`CLAUDE.md` documentation exists for this spec yet.**
  **Still correctly deferred** — not because a deploy hasn't happened
  (it has), but because the *current* deploy is a deliberate, temporary
  no-reservation bridge state (see below), not the fully-audited final one.
  Writing the runbook now would document a state that is about to change
  again.
- **F2 is now resolved (2026-09-02)** — the image builds locally and
  `api.handler.handler`/`contracts.card` import and run inside it, **and**
  (2026-09-03) a real AWS Lambda invoke via the deployed API Gateway URL now
  also succeeds — the Lambda Runtime Interface Client's own request loop,
  cold start against the real `public.ecr.aws/lambda/python:3.12` runtime,
  and the CDK-provisioned IAM role are all now real-world confirmed, not just
  locally smoke-tested.
- **NEW (2026-09-03) — the deployed Lambda is currently running WITHOUT its
  audited `reserved_concurrent_executions=5`.** It was deployed with `-c
  feed_api_reserved_concurrency=none` (see the 2026-09-02 override-mechanism
  Audit Log entry) because this account's Lambda concurrency quota (10, not
  AWS's stated default of 1000) could not yet support the reservation. AWS
  Support case `178836416700301` (increase to 1001) is still `CASE_OPENED`.
  **A future reader of this document should not mistake the current live
  deploy for AD-7's fully-audited final state** — it is a deliberate,
  temporary bridge, closed only once a plain `cdk deploy AiRadarFeedApi`
  (no context override) redeploys with the reservation restored.
