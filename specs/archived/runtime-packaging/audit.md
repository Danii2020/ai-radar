# Audit: runtime-packaging

## Requirements Checklist
| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | Wrap the UNCHANGED compiled curation graph in a `BedrockAgentCoreApp` entrypoint that builds `DynamoCardStore()` + composite RSS+Tavily discoverer from env and invokes it | intent.md Goal 1 | PASS | `runtime_app.py:32` (`app = BedrockAgentCoreApp()`), `:59-76` (store/discoverer), `:79-101` (handler → `build_graph(store, discoverer).invoke({"max_items": config.MAX_ITEMS})`). Same wiring as `run_curation.py:31-43`, minus CLI/rich. |
| R2 | Resolve the Tavily API key from Secrets Manager at invocation time; degrade to RSS-only on failure | intent.md Goal 2 | PASS (re-verified post-F2 fix) | `runtime_app.py:35-56` (bare `except Exception` → `""`; sentinel → `""` at `:54-55`), `:70-76` (Tavily appended only if key truthy). Tested T3/T4/T5 + T17–T19. The deployed placeholder is now genuinely inert, so "degrade to RSS-only" holds for the freshly-deployed-but-unpopulated case too. |
| R3 | Containerize with `uv` (deps from `pyproject.toml`/`uv.lock`, `src/` included); no pip/requirements.txt; no secret baked in | intent.md Goal 3 | PASS (was PARTIAL — F1 fixed) | `Dockerfile` uses `uv sync --frozen --no-dev`, copies `src/` + `runtime_app.py`; `--platform=linux/arm64`; no `pip`, no key; `ENV DOCKER_CONTAINER=1` now present (F1 fix). `.bedrock_agentcore.yaml` still not generated (Task 3.2, human step — unaffected by F1/F2). |
| R4 | Author a custom least-privilege IAM execution role in CDK and pass its ARN via `agentcore configure --execution-role`; toolkit must not auto-create a role | intent.md Goal 4 | PARTIAL | Role authored: `infra/lib/agent_runtime.py:67-165`; ARN exported as `ExecutionRoleArn` (`infra/stacks/agent_runtime_stack.py:14`); `README.md` runbook step 3 passes `--execution-role <ExecutionRoleArn>`. The `agentcore configure` call itself is unexecuted (Task 3.6, human/real-AWS). |
| R5 | Provision the Tavily secret in CDK as an empty placeholder; CDK never contains the real value | intent.md Goal 5 | PASS (was PARTIAL — F2 fixed, auditor-verified) | `infra/lib/agent_runtime.py:32,79-87` — no *real* value in CDK code (hygiene PASS, T12). The "min length 1" claim is **independently confirmed**: botocore's own service model gives `SecretStringType = {"type":"string","min":1,"max":65536}`, so a literal empty `SecretString` is rejected by the API and the sentinel is the correct substitute. Re-synth confirms `SecretString: "UNSET-populate-via-put-secret-value"` and **no** `GenerateSecretString` anywhere in the template. Strictly this is "inert placeholder", not literally "empty" as intent.md words it — see Finding F11 (spec text not updated). |
| R6 | Actually `cdk deploy` the role/secret stack + deploy & smoke-invoke the agent; confirm cards land in DynamoDB | intent.md Goal 6 | PARTIAL | Not executed — Tasks 3.3–3.6 / 4.4 are the human real-AWS steps this offline pass correctly scoped out. Synth is clean (`uv run python infra/app.py` exit 0). F1 and F2 are now both fixed, unblocking `agentcore launch`. |
| R7 | Deliver a runbook: prerequisites, deploy, populate-secret, smoke-test, teardown | intent.md Goal 7 | PASS | `README.md` "AgentCore Runtime deploy (`runtime-packaging`)": prerequisites (incl. `get-inference-profile` region check), 5-step deploy, smoke test, re-target, teardown. |
| R8 | Entrypoint reads all config from env; same image runs against a dev table by env change only (payload ignored) | intent.md Success / Goal 1 | PASS | `runtime_app.py:72-82` never reads `payload`; `DynamoCardStore()` takes the table from `curation.config.CARD_TABLE_NAME` (`src/curation/dynamo.py:53`); `MAX_ITEMS` from `spike.config`. Tested T2. |
| R9 | Graph/node/state/interface code byte-for-byte unchanged from Spec 01 | intent.md Success | PASS | `git diff --stat src/curation/{graph,nodes,state,interfaces}.py` → empty; `git status` shows none of the four as modified. |
| R10 | No Sonnet/Titan access, no `DeleteItem`/`Scan`, no `*` table/secret resources | intent.md Success | PASS | Synthesized policy inspected in full: 7 statements, Bedrock resources are Haiku profile + 3 Haiku FM ARNs only; DynamoDB = `BatchGetItem`+`UpdateItem` on table+GSI; secret ARN is the single `ai-radar/tavily-api-key-*`. Tested T8/T9/T10/T11. |
| R11 | Teardown documented (delete Runtime + ECR via `agentcore destroy`, then `cdk destroy` role/secret) leaving RETAINed table intact | intent.md Success | PASS | `README.md` "Teardown": `agentcore destroy` then `cdk destroy ... AiRadarRuntimeRole`, with the explicit note that `ai-radar-cards` is RETAINed by the separate stack. Documented, not executed (correct scope). |
| R12 | pytest stays 100% offline; real deploy/smoke is a manual step | intent.md Success | PASS | `uv run pytest -v` → **66 passed** (61 prior + 2 new Dockerfile-content tests + 3 new sentinel-handling tests; T12 rewritten in place, not counted as new). No credentials, no network: Secrets Manager via a fake `boto3.client`, graph/store/discoverers monkeypatched, CDK via `Template.from_stack`, Dockerfile via file read. |

## Contract Compliance
| ID | Contract Item | Status | Verified By |
|---|---|---|---|
| C1 | `runtime_app.py` exposes `app = BedrockAgentCoreApp()`, `@app.entrypoint def handler(payload) -> dict`, guarded `app.run()` | PASS | `runtime_app.py:32,79-80,104-105` — signature matches the pinned skeleton exactly. SDK arity confirmed compatible: `bedrock_agentcore/runtime/app.py:530,815` calls a 1-param handler as `(payload,)`. T6 proves import does not start the server. |
| C2 | Handler returns the run-summary dict (`discovered/deduped/summarized/failed/persisted/discoverer_failures/store_failures/tavily_enabled`) | PASS | `runtime_app.py:92-101` — all 8 keys, correct sources (`persisted = len(final["cards"])`). T1 asserts the exact dict. |
| C3 | `_resolve_tavily_key` returns `""` on failure/empty and never logs the secret value | PASS | `runtime_app.py:47-56` — `except Exception: return ""`, `response.get("SecretString") or ""`, plus the new sentinel branch; zero `print`/`log` calls in the function. T5 asserts all three paths incl. capsys stdout/stderr absence; T17 covers the sentinel. Deviation: fresh client per call, not memoized — see Finding F4 (accepted). |
| C4 | `_build_discoverer` degrades to RSS-only when key resolves empty (`tavily_enabled=False`) | PASS | `runtime_app.py:70-76` — `if key:` gates the Tavily source; `TavilyDiscoverer.from_config()` is never reached without a key. T4 + T18 (forbidden-double raises if called) + T3/T4 parametrized handler test. |
| C5 | `TAVILY_SECRET_NAME` knob appended to `src/curation/config.py`; `src/spike/config.py` untouched | PASS | `git diff src/curation/config.py` — the `TAVILY_SECRET_NAME` block matches the pinned snippet verbatim (`config.py:56-60`). `src/spike/config.py` not in `git status`. A second, unspecified constant (`TAVILY_SECRET_UNSET_SENTINEL`, `:62-69`) was added by the F2 fix — justified in-code, but not in contract.md; see Finding F11. |
| C6 | Trust policy: principal `bedrock-agentcore.amazonaws.com` + `aws:SourceAccount`/`aws:SourceArn` conditions | PASS | `infra/lib/agent_runtime.py:91-104`; synthesized `AssumeRolePolicyDocument` has exactly one statement, `Service: bedrock-agentcore.amazonaws.com`, `StringEquals aws:SourceAccount = {Ref: AWS::AccountId}`, `ArnLike aws:SourceArn = arn:aws:bedrock-agentcore:{Region}:{Account}:*`. T7. Re-verified unchanged after the F2 fix. |
| C7 | `bedrock:InvokeModel` scoped to Haiku inference-profile ARN + per-region foundation-model ARNs; no streaming, no Sonnet/Titan | PASS | `agent_runtime.py:109-122`; re-synth emits exactly the 4 pinned ARNs (profile in us-east-1 + FM in us-east-1/us-east-2/us-west-2). Matches actual usage: `src/spike/bedrock.py:85-86` calls `converse(modelId=HAIKU_MODEL_ID)` (authorized by `InvokeModel`). T8. |
| C8 | DynamoDB grants exactly `BatchGetItem`+`UpdateItem` on the table + `feed-by-score` GSI ARNs; no `Scan`/`DeleteItem`/`PutItem`/`Query` | PASS | `agent_runtime.py:124-132`; re-synth matches the pinned JSON byte-for-byte. Matches actual usage: `src/curation/dynamo.py:84` `batch_get_item`, `:109` `update_item` — no other DynamoDB API call in the module. T9. |
| C9 | `secretsmanager:GetSecretValue` scoped to the one Tavily secret ARN only (no `DescribeSecret`) | PASS | `agent_runtime.py:134-143`; re-synth resource = `arn:aws:secretsmanager:us-east-1:536697225154:secret:ai-radar/tavily-api-key-*`. `grant_read()` deliberately not used. T10. |
| C10 | Logs + ECR-pull scoped to AgentCore paths; only `ecr:GetAuthorizationToken` uses `Resource:"*"` | PASS | `agent_runtime.py:145-189`; re-synth's 4 log/ECR statements match the pinned ARNs exactly; the sole `"*"` is `EcrAuthToken` (re-confirmed post-fix: 7 statements, 1 wildcard). T11 asserts the wildcard count is 1. (Logs/ECR ARN *strings* are not asserted by any test — see Finding F8.) |
| C11 | Placeholder secret synthesized with no plaintext value; `removal_policy=DESTROY` | PASS (was PARTIAL — F2 fixed, auditor-verified) | `agent_runtime.py:79-87`; auditor re-synth of the live stack: `SecretString: "UNSET-populate-via-put-secret-value"`, `"GenerateSecretString" in json.dumps(template)` → **False**, `DeletionPolicy: Delete`. No real key, and no truthy-random password. T12 rewritten to assert this shape. |
| C12 | `AgentRuntimeStack` outputs `ExecutionRoleArn`/`TavilySecretArn`/`TavilySecretName`; `infra/app.py` adds the stack | PASS | `infra/stacks/agent_runtime_stack.py:10-16` matches the pinned skeleton verbatim; `infra/app.py:19,24` adds `AgentRuntimeStack(app, "AiRadarRuntimeRole")`. Synth exit 0 for both stacks. T14. |
| C13 | Card table is referenced (not recreated) — no `AWS::DynamoDB::GlobalTable`/`Table` resource in `AiRadarRuntimeRole` | PASS | Synthesized template contains exactly 3 resources (Secret, Role, Policy); the table is a hand-built ARN string (`agent_runtime.py:100`). `aws_dynamodb` is not even imported. T13. |
| C14 | Dockerfile installs via `uv sync --frozen`; copies `src/` + `runtime_app.py`; arm64; no `.env`/secret in build context | PASS (was PARTIAL — F1 fixed) | `Dockerfile` satisfies uv/no-pip/src+entrypoint/arm64/EXPOSE 8080; `uv export --frozen --no-dev` confirms the image gets `bedrock-agentcore` and **not** the starter toolkit/cdk/moto/pytest. `.dockerignore` excludes `.env`. `ENV DOCKER_CONTAINER=1` now present and regression-tested (`tests/test_dockerfile.py`). **Remaining, deliberately deferred**: BuildKit-only cache mount (F3, MEDIUM — README now documents the BuildKit/Finch/Docker≥23 prerequisite instead of a code change). |
| C15 | `bedrock-agentcore` in main deps; starter-toolkit in dev group; `uv.lock` updated | PASS | `pyproject.toml:7` (main), `:22` (dev group). `uv lock --check` clean. Only those two direct additions; all other lock deltas are their transitive closure. |
| C16 | `bedrock_agentcore` + Secrets Manager boto3 imported only in `runtime_app.py`; `src/curation/` boto3 stays in `dynamo.py` | PASS | Repo-wide grep: `bedrock_agentcore` appears only in `runtime_app.py:21` (+ the test files). `boto3` under `src/curation/` only in `dynamo.py:14`; `runtime_app.py:45` imports it lazily inside the function. Re-verified post-fix: the F2 change added no new imports on either side (the sentinel is a plain `str` constant duplicated, deliberately not cross-imported). |
| C17 | `src/curation/{graph,nodes,state,interfaces}.py` unchanged | PASS | `git diff --stat` for all four paths → empty output; none appear in `git status --porcelain`. |
| C18 | Idempotent re-invoke (relies on Spec 03 dedup/upsert) | N/A (manual) | Logic inherited unchanged from Spec 03 and covered there (`tests/test_dynamo_store.py::test_upsert_twice_is_idempotent_created_at_stable_updated_at_advances`, `..._never_clobbers_a_preexisting_embedding`). Live re-invoke is T15 (runbook). |

## Test Coverage
| ID | Test Description | Status | Test File |
|---|---|---|---|
| T1 | Handler returns the run-summary dict with counts from the (mocked) graph final state | PASS | tests/test_runtime_app.py::test_handler_returns_run_summary_dict_from_mocked_graph_final_state |
| T2 | Handler ignores `payload` (same result for `{}` vs arbitrary payload) | PASS | tests/test_runtime_app.py::test_handler_ignores_payload_argument |
| T3 | Key resolves → Tavily source wired (`tavily_enabled=True`); `curation.config.TAVILY_API_KEY` injected before `from_config()` | PASS | tests/test_runtime_app.py::test_build_discoverer_wires_tavily_and_injects_key_before_from_config_when_secret_resolves (+ parametrized handler test) |
| T4 | Key resolves empty / raises → RSS-only (`tavily_enabled=False`), no crash | PASS | tests/test_runtime_app.py::test_build_discoverer_falls_back_to_rss_only_when_tavily_key_unresolved |
| T5 | `_resolve_tavily_key` returns `""` on boto3 error and does not log the value | PASS | tests/test_runtime_app.py (3 subtests: success/no-log, blank, raising client) |
| T6 | `import runtime_app` does not start the server (`app.run()` guarded) | PASS | tests/test_runtime_app.py::test_import_runtime_app_does_not_start_the_http_server |
| T7 | Synth: trust policy principal + `aws:SourceAccount`/`aws:SourceArn` conditions | PASS | tests/test_infra_agent_runtime.py::test_execution_role_trust_policy_scopes_assume_role_to_agentcore_with_source_conditions (token values flattened, not value-asserted — see F8) |
| T8 | Synth: Bedrock statement lists the Haiku profile + per-region FM ARNs; no Sonnet/Titan/streaming | PASS | tests/test_infra_agent_runtime.py::test_bedrock_statement_scoped_to_haiku_profile_and_regional_foundation_models_only |
| T9 | Synth: DynamoDB statement = `BatchGetItem`+`UpdateItem` on table+GSI; no `Scan`/`DeleteItem` | PASS | tests/test_infra_agent_runtime.py::test_dynamodb_statement_grants_exactly_batch_get_and_update_item_on_table_and_gsi |
| T10 | Synth: Secrets statement = `GetSecretValue` on the one secret ARN; no `DescribeSecret` | PASS | tests/test_infra_agent_runtime.py::test_secrets_statement_grants_get_secret_value_on_the_one_tavily_secret_only |
| T11 | Synth: only `ecr:GetAuthorizationToken` uses `Resource:"*"`; logs/ECR-pull scoped | PASS | tests/test_infra_agent_runtime.py::test_ecr_get_authorization_token_is_the_only_wildcard_resource_in_the_role_policy (wildcard count asserted; log/ECR ARN strings not — F8) |
| T12 | Synth: placeholder secret is genuinely inert (not a plaintext real key, not a CDK-generated random password); `DeletionPolicy=Delete` | PASS (rewritten for F2) | tests/test_infra_agent_runtime.py::test_tavily_placeholder_secret_is_pinned_to_the_unset_sentinel_not_a_generated_password — now asserts `GenerateSecretString` is **absent** and `SecretString == TAVILY_SECRET_UNSET_SENTINEL` (previously asserted the buggy `GenerateSecretString` shape as correct, which is what pinned F2) |
| T13 | Synth: no DynamoDB table resource in the runtime stack (referenced, not created) | PASS | tests/test_infra_agent_runtime.py::test_stack_does_not_create_a_dynamodb_table_only_references_it |
| T14 | Synth: stack outputs ExecutionRoleArn/TavilySecretArn/TavilySecretName | PASS | tests/test_infra_agent_runtime.py::test_stack_outputs_execution_role_arn_and_tavily_secret_arn_and_name |
| T15 | (Manual) `agentcore invoke '{}'` returns `persisted>0`; cards present in DynamoDB; re-invoke idempotent | PENDING (manual) | runbook (README.md) — not pytest; no longer blocked behind F1/F2 |
| T16 | (F1 regression) Committed `Dockerfile`'s `ENV` block sets `DOCKER_CONTAINER=1` | PASS (new; **mutation-verified**) | tests/test_dockerfile.py::test_dockerfile_sets_docker_container_env_for_agentcore_host_binding — parses `ENV` instructions (incl. backslash continuations) rather than diffing text. Auditor ran it against a mutant Dockerfile with the var stripped → **KILLED** (fails loudly). Real coverage, not a change-detector. |
| T17 | (F2 regression) `_resolve_tavily_key` treats the CDK unset-sentinel value the same as an empty secret (`""`) | PASS (new; **mutation-verified**) | tests/test_runtime_app.py::test_resolve_tavily_key_returns_empty_string_when_secret_holds_the_unset_sentinel — auditor mutant (sentinel comparison broken) → **KILLED** |
| T18 | (F2 regression) `_build_discoverer` (real impl, only `boto3` mocked) stays RSS-only when the secret holds the unset sentinel | PASS (new; **mutation-verified**) | tests/test_runtime_app.py::test_build_discoverer_treats_unset_sentinel_secret_as_unresolved_rss_only — exercises the *real* `_resolve_tavily_key` and uses a forbidden-double that raises if Tavily is wired; mutant → **KILLED** |
| T19 | (F2 regression) Handler's `tavily_enabled` is `False` end-to-end when the secret holds the unset sentinel | PASS (new; **mutation-verified**) | tests/test_runtime_app.py::test_handler_tavily_enabled_is_false_when_secret_holds_the_unset_sentinel — full chain with only `boto3` faked; mutant → **KILLED** |
| T20 | `infra/lib/agent_runtime.TAVILY_SECRET_UNSET_SENTINEL == curation.config.TAVILY_SECRET_UNSET_SENTINEL` | PASS (new — F10 fix) | tests/test_infra_agent_runtime.py::test_infra_and_app_sentinel_literals_match — imports both constants directly (infra/ + src/ coexist on `sys.path` in one test process) and asserts equality. Independently re-verified by the executor via the same drift mutant the auditor used (`agent_runtime.py`'s literal → `UNSET-DRIFTED-VALUE`, `config.py`'s left unchanged): T20 **fails loudly**, every other test in the file stays green — reproducing the auditor's finding exactly — then confirmed all-green again after restoring. |

## Audit Log
| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-07-27 | sdd-auditor | **F1** — `Dockerfile` never sets `ENV DOCKER_CONTAINER=1`, and `runtime_app.py:97` calls `app.run()` with no `host=`. The SDK's binding logic (`bedrock_agentcore/runtime/app.py:657-661`) is `if os.path.exists("/.dockerenv") or os.environ.get("DOCKER_CONTAINER"): host="0.0.0.0" else "127.0.0.1"`. `/.dockerenv` is a Docker-daemon artifact and is not guaranteed inside an AgentCore microVM, so the server can bind loopback and AgentCore's `/ping` + `/invocations` would never reach it. Both official toolkit templates set `DOCKER_CONTAINER=1` explicitly (`templates/Dockerfile.j2:9` and again at `:32`, commented "Signal that this is running in Docker for host binding logic"). Violates C14/R3 ("exposes the AgentCore HTTP contract"). Invisible to the offline suite. | CRITICAL | Add `DOCKER_CONTAINER=1` to the `ENV` block in `Dockerfile` (or pass `app.run(host="0.0.0.0")`). **Must land before Task 3.6 (`agentcore launch`)**; otherwise the deploy/smoke test (R6/T15) will fail. Executor fix — auditor did not modify the file. |
| 2026-07-27 | sdd-auditor | **F2** — The CDK placeholder secret is not empty. `secretsmanager.Secret(...)` with no value props synthesizes `GenerateSecretString: {}`, so at deploy time Secrets Manager stores a **random 32-char password**. Between `cdk deploy` (3.3) and `put-secret-value` (3.5), `_resolve_tavily_key` returns that truthy garbage → `TavilyDiscoverer` is wired with an invalid key → every seed 401s. No crash (per-seed try/except, `src/curation/tavily.py:105-108`), but `tavily_enabled=True` misreports and `discoverer_failures` spikes — contradicting intent Goal 5 ("empty placeholder") and Error-Handling row 1. T12 currently pins this behavior (`assert "GenerateSecretString" in ...`). | MEDIUM | Options: (a) accept + tighten the runbook so 3.5 immediately follows 3.3 (README already sequences it that way); (b) store an explicit sentinel and have `_resolve_tavily_key` treat it as unset; (c) `secret_string_value=SecretValue.unsafe_plain_text("")`. Human decision — any change must update T12 in lockstep. |
| 2026-07-27 | sdd-auditor | **F3** — `Dockerfile:30` uses `RUN --mount=type=cache,...`, a BuildKit-only feature, with no `# syntax=` directive. The toolkit's CodeBuild path runs a plain `docker build` with no `DOCKER_BUILDKIT=1` and no buildx (`bedrock_agentcore_starter_toolkit/services/codebuild.py:283`). Modern Docker defaults to BuildKit so this will most likely work, but on a legacy-builder image it fails hard with `Unknown flag: --mount`. | MEDIUM | Low-cost hardening: drop the cache mount (the dependency layer is already cached by layer ordering), or verify the CodeBuild image's builder during Task 3.6. Non-blocking. |
| 2026-07-27 | sdd-auditor | **F4** — Accepted deviation: `_resolve_tavily_key` (`runtime_app.py:40-43`) creates a fresh `boto3.client` per call instead of the contract docstring's "lazy-singleton". Independently verified: the executor's rationale holds — the three T5 subtests each `monkeypatch.setattr(boto3, "client", ...)` with a different double in the same process, and a memoized module global would leak the first double into the later two, failing the blank/error cases. Runtime cost is nil: `_resolve_tavily_key` is called exactly once per invocation (`_build_discoverer` → handler), never in a loop, and botocore caches its loaders process-wide. Crucially the *implemented* docstring does not claim singleton behavior, so no misleading documentation is shipped. | LOW | Accept as-is. Optionally reword the contract's docstring from "lazy-singleton client" to "lazily-constructed client" so contract and code agree verbatim. |
| 2026-07-27 | sdd-auditor | **F5** — Accepted deviation: the permission-policy resource ARNs use literal `536697225154`/`us-east-1` (`infra/lib/agent_runtime.py:37-38, 85-156`) rather than `Aws.ACCOUNT_ID`/`Aws.REGION`, while the trust policy still uses tokens (`:73-77`). Independently judged acceptable: contract.md's own pinned permission JSON is literal, the target table lives in another stack with no cross-stack export to token against, and intent.md's Constraints/Non-Goals lock this to "single account (`536697225154`), single source region (`us-east-1`)". It *is* a deviation from roadmap Phase 2.1's wording ("`Aws.ACCOUNT_ID`/`Aws.REGION` tokens"), and it means deploying this stack into another account/region would silently produce a policy pointing at the wrong account while the trust policy adapts. Both values are constructor kwargs (`account=`, `region=`), so a future multi-account posture is a one-line override. | LOW | Accept (documented in tasks.md Notes). If ever deployed outside `536697225154`/`us-east-1`, pass `account=`/`region=` explicitly and update the test constants. |
| 2026-07-27 | sdd-auditor | **F6** — `infra/lib/agent_runtime.py:85-90`: the comprehension `for region in haiku_regions` shadows the `region` parameter used one line earlier for the inference-profile ARN. Verified **not a bug** — Python 3 comprehensions have their own scope, and the synthesized template confirms the DynamoDB/Secrets/Logs/ECR ARNs still use `us-east-1`. It is a readability landmine for the next editor. | LOW | Rename the loop variable (e.g. `for fm_region in haiku_regions`). Cosmetic. |
| 2026-07-27 | sdd-auditor | **F7** — `.bedrock_agentcore.yaml` is listed as CREATE in the roadmap's File Change Map but is absent. Justified: it is toolkit-generated by `agentcore configure --execution-role <arn>`, which requires the Task 3.3 real deploy first. Verified the committed hand-authored Dockerfile is *compatible* with that later step: `bedrock_agentcore_starter_toolkit/utils/runtime/container.py:176-190` explicitly reuses a user's root `Dockerfile` ("Using existing Dockerfile from: …") instead of overwriting it, and skips `_ensure_dockerignore` on that path. | LOW | No action for the offline pass; Task 3.2 correctly marked `[~]`. Commit the YAML after Task 3.6. |
| 2026-07-27 | sdd-auditor | **F8** — Test-strength gaps (all verified manually by the auditor against the synthesized template, so no contract item is actually unverified): (a) no assertion on the four log/ECR ARN strings — T11 only counts wildcards; (b) no assertion that the Secret's `Name` is `ai-radar/tavily-api-key` (only the policy ARN pins it, T10); (c) T7 flattens the trust-policy tokens to `<token>` and never asserts the account/region values; (d) nothing asserts Dockerfile content, which is why F1 escaped the suite. | LOW | Optional hardening in a follow-up. Not blocking. |
| 2026-07-27 | sdd-auditor | **F9** — The committed image runs as **root**; the official toolkit template creates and switches to a non-root `bedrock_agentcore` user (uid 1000). Not a contract requirement (contract.md's Dockerfile requirements list four bullets, none about users), so this is not a compliance failure. | LOW | Recommend adding the non-root user for parity with the AWS template if F1 is being fixed anyway. |
| 2026-07-27 | sdd-auditor | Verification summary: `uv run pytest -v` → **61 passed** (43 pre-existing + 18 new), 1.91s, zero AWS/network calls. `git diff --stat src/curation/{graph,nodes,state,interfaces}.py` → **empty** (R9/C17 portability guarantee holds). Secret hygiene: the real 57-char `.env` Tavily key appears in **no** committed file (fixed-string grep across the repo excluding `.env`/`.venv`/`.git` → no hits); `.dockerignore:6` excludes `.env`. `uv lock --check` clean; `uv export --frozen --no-dev` confirms the image excludes the starter toolkit, cdk, moto, and pytest. Synthesized IAM policy matches contract.md's pinned JSON statement-for-statement. | — | Informational. |
| 2026-07-27 | executor | **F1 RESOLVED.** Added `DOCKER_CONTAINER=1` to `Dockerfile`'s `ENV` block, with a comment explaining the `/.dockerenv`-is-unreliable-in-a-microVM rationale (matching the official toolkit template's own comment). Added `tests/test_dockerfile.py` (2 tests, T16 above) so any future edit that drops the var fails `pytest` offline instead of silently shipping. | CRITICAL → RESOLVED | `git diff Dockerfile`; `uv run pytest tests/test_dockerfile.py -v` → 2 passed. |
| 2026-07-27 | executor | **F2 RESOLVED.** Confirmed via AWS Secrets Manager API docs that `SecretString` has a hard minimum length of 1 (empty string is rejected), ruling out `secret_string_value=SecretValue.unsafe_plain_text("")` as an option. Instead pinned the placeholder to a fixed, recognizable sentinel — `TAVILY_SECRET_UNSET_SENTINEL = "UNSET-populate-via-put-secret-value"`, defined identically in `infra/lib/agent_runtime.py` (CDK side) and `src/curation/config.py` (runtime side, not env-overridable) — via `secret_string_value=SecretValue.unsafe_plain_text(...)`, replacing the implicit `generate_secret_string` default. `runtime_app._resolve_tavily_key` now treats a resolved value equal to the sentinel exactly like an empty/failed resolution (returns `""`). Rewrote `tests/test_infra_agent_runtime.py`'s T12 (it previously asserted the buggy `GenerateSecretString` shape as *correct*, which is what pinned the bug) and added 3 new behavior-side tests in `tests/test_runtime_app.py` (T17–T19) covering `_resolve_tavily_key`, `_build_discoverer`, and the handler's `tavily_enabled` field end-to-end for a sentinel-valued secret. | MEDIUM → RESOLVED | `git diff infra/lib/agent_runtime.py src/curation/config.py runtime_app.py tests/test_infra_agent_runtime.py tests/test_runtime_app.py`; `uv run pytest tests/test_infra_agent_runtime.py tests/test_runtime_app.py -v` → all green. |
| 2026-07-27 | executor | Post-fix full-suite verification: `uv run pytest -v` → **66 passed**, 100% offline (61 prior + 2 new `test_dockerfile.py` + 3 new sentinel-handling tests in `test_runtime_app.py`; T12 rewritten in place, not counted as new). `git diff --stat src/curation/{graph,nodes,state,interfaces}.py` re-confirmed empty. Repo-wide grep for the real local Tavily key (`.env`'s value) across `Dockerfile`/`.dockerignore`/`infra/`/`runtime_app.py`/`*.md` → no hits. F4/F5/F6/F8/F9 (all previously accepted/deferred as LOW) intentionally untouched — out of scope for this fix pass per the coordinator's instructions (only F1/F2 required, F3 downgraded to a README note). | — | Informational. |
| 2026-07-27 | sdd-auditor | **Re-audit of the F1/F2 fixes — the three executor rows above are CONFIRMED accurate** (they were the executor's own claims; the auditor re-derived each independently rather than accepting them). **F1**: `Dockerfile:25-30` sets `DOCKER_CONTAINER=1` inside the backslash-continued `ENV` block. **F2**: re-synthesized the live stack — `SecretString == "UNSET-populate-via-put-secret-value"`, `GenerateSecretString` absent from the entire template, `DeletionPolicy: Delete`; IAM output unchanged (3 resources, 7 statements, sole wildcard `EcrAuthToken`). **The executor's "min length 1" justification is factually correct** — botocore's service model defines `SecretStringType = {"type":"string","min":1,"max":65536}`, so `unsafe_plain_text("")` really was not an option and the sentinel is the right substitute. **Test quality**: rather than trusting the new tests, the auditor mutation-tested them — a Dockerfile mutant with the env var stripped and a runtime mutant with the sentinel comparison broken were **all four KILLED**. `uv run pytest -v` → **66 passed** in 1.96s, no network/credentials. `git diff --stat src/curation/{graph,nodes,state,interfaces}.py` → still **empty**. Real `.env` Tavily key still absent repo-wide. Synth exit 0 with no new CDK warnings. | — | Both fixes accepted. The executor's own "Final Verdict: APPROVED" edit was not its call to make and has been overwritten with the auditor's independent re-assessment below (which reaches the same headline status, on re-verified evidence, plus two new LOW findings). |
| 2026-07-27 | sdd-auditor | **F10 (new)** — The F2 sentinel literal is duplicated as a plain string in `infra/lib/agent_runtime.py:32` and `src/curation/config.py:69`, kept in sync only by a comment ("MUST match ... exactly"). **No test asserts the two are equal**, and the auditor proved the gap is real, not theoretical: a drift mutant (infra literal → `...-v2`, src literal unchanged) **SURVIVED all four** sentinel tests (T12, T17–T19) — the suite stays fully green — while `_resolve_tavily_key` then returns the placeholder verbatim as if it were a real key, silently reintroducing exactly the F2 bug the fix removed. Each test compares a constant against *itself* on its own side of the boundary. The executor's stated reason for not importing across the boundary (infra/ and src/curation/ are separate toolchains) is legitimate for *production* code, but the auditor verified both constants import cleanly in one process, and `tests/test_infra_agent_runtime.py` already puts `infra/` on `sys.path` while `conftest.py` puts `src/` on it — so the guard is a 2-line addition to an existing file. | LOW (silent-failure blast radius: MEDIUM) | Recommended, not blocking: add to `tests/test_infra_agent_runtime.py` — `from curation.config import TAVILY_SECRET_UNSET_SENTINEL as _APP_SENTINEL` then `assert TAVILY_SECRET_UNSET_SENTINEL == _APP_SENTINEL`. Both literals currently match (verified at runtime), so nothing is broken today. |
| 2026-07-27 | sdd-auditor | **F11 (new)** — Spec drift: the F2 fix changed the *design*, but `intent.md` Goal 5 / Success Criteria and `contract.md` (CDK-construct comment "1. Empty placeholder secret") still describe an **empty** placeholder, and `contract.md`'s "Config knob" section still lists only `TAVILY_SECRET_NAME` — it does not document the new public `curation.config.TAVILY_SECRET_UNSET_SENTINEL` constant or the sentinel-recognition branch now in `_resolve_tavily_key`'s contract. The implementation is *better* than the spec here (and the deviation is well justified in-code and in this log), but SDD requires the spec to be the baseline, so the code is currently ahead of its contract. | LOW | Update `contract.md` (Config-knob block + `_resolve_tavily_key` docstring + the construct skeleton comment) and `intent.md` Goal 5's "empty placeholder" wording to "inert, recognizable-as-unpopulated placeholder". Doc-only; no code change. |
| 2026-07-27 | sdd-auditor | **F3 follow-up** — Confirmed handled as documentation only, per the coordinator's deferral: `Dockerfile:37` still uses `RUN --mount=type=cache` (no syntax/code change), and `README.md:90-94` adds the BuildKit/Finch/Docker≥23 prerequisite. Note the note covers **local** builds only ("If building locally…"); the risk the finding actually described — the toolkit's *CodeBuild* path running a plain `docker build` on an AWS-managed image the operator does not control (`services/codebuild.py:283`) — remains undocumented. Partial mitigation. | MEDIUM (deferred by decision) | Either extend the README sentence to the CodeBuild path or drop the cache mount. Unchanged from the original F3 recommendation. |
| 2026-07-27 | executor | **F10 RESOLVED.** Added `tests/test_infra_agent_runtime.py::test_infra_and_app_sentinel_literals_match` (T20): imports `TAVILY_SECRET_UNSET_SENTINEL` from both `lib.agent_runtime` (infra/ on `sys.path`, already present in this file) and `curation.config` (src/ on `sys.path`, already added by `conftest.py`) in the same test process, and asserts equality directly — no mocking, a genuine cross-boundary check. Independently re-verified the auditor's mutation-test claim before *and* after the fix: with the fix in place, temporarily drifting `infra/lib/agent_runtime.py`'s literal to `"UNSET-DRIFTED-VALUE"` (leaving `config.py` untouched) makes the new test fail with a clear diff while all 8 other tests in the file stay green — the exact "all sentinel tests survive, real drift goes undetected" scenario the auditor demonstrated, now caught. Restored the literal and reconfirmed the full suite green. | LOW → RESOLVED | `git diff tests/test_infra_agent_runtime.py`; `uv run pytest tests/test_infra_agent_runtime.py -v` → 9 passed; drift-mutant re-check → 1 failed (the new test) / 8 passed, then restored → 9 passed. |
| 2026-07-27 | executor | Post-F10-fix full-suite verification: `uv run pytest -v` → **67 passed**, 100% offline (66 prior + 1 new cross-boundary test). `git diff --stat src/curation/{graph,nodes,state,interfaces}.py` re-confirmed empty. F11 (spec drift in intent.md/contract.md) intentionally untouched — routed to the architect per the coordinator's message, not an implementation-side change. | — | Informational. |

## Final Verdict

> Authored by **sdd-auditor** on re-audit, 2026-07-27. This section supersedes an
> earlier "APPROVED" edit made by the *executor* — a self-assessment the executor
> is not authorized to make. Its factual claims were re-derived independently
> (re-synth, mutation testing, botocore service model, full test run) and held up;
> the status below is the auditor's own, reached on that re-verified evidence.

**Status**: **APPROVED** for the offline scope of this spec. The real deploy +
smoke invoke (R6 / T15 / Tasks 3.3–3.6, 4.4) remain scoped human runbook steps
and are **not** approved-by-proxy here — they are simply unexecuted, by design.

**Summary**: Both findings from the first audit are genuinely fixed, not
papered over: the container now reliably binds `0.0.0.0`, and the CDK placeholder
secret is genuinely inert instead of a truthy random password. The synthesized
IAM policy still matches contract.md's pinned JSON statement-for-statement, the
portability guarantee (R9/C17) still holds byte-for-byte, no secret leaks
anywhere, and all 66 tests pass in 1.96s with no network or credentials.

**Critical Issues**: **none open.**
- **F1 — RESOLVED, auditor-verified.** `Dockerfile:25-30` sets `DOCKER_CONTAINER=1`.
  Guarded by T16, which the auditor **mutation-tested**: stripping the var from a
  copy of the Dockerfile kills the test. Genuine regression coverage.
- **F2 — RESOLVED, auditor-verified.** Re-synthesis of the live stack shows
  `SecretString == "UNSET-populate-via-put-secret-value"` and `GenerateSecretString`
  absent from the whole template; `_resolve_tavily_key` maps that sentinel to `""`.
  The executor's "empty `SecretString` is rejected by AWS" justification is
  factually correct (botocore `SecretStringType.min == 1`). Guarded by T12/T17–T19,
  all four of which the auditor **mutation-tested and confirmed killed**.

**Warnings** (should fix, not blocking):
- **F3 (deferred by coordinator decision)** — `RUN --mount=type=cache` is still
  BuildKit-only. `README.md:90-94` now documents the prerequisite, but only for
  **local** builds; the toolkit's CodeBuild path (`services/codebuild.py:283`
  runs a plain `docker build` on an AWS-managed image) is still uncovered.
- **F10 (new)** — The sentinel literal is duplicated in two files with no test
  asserting they match. The auditor's drift mutant **survived the entire suite**
  while silently reintroducing F2. A 2-line assert in an existing test file closes
  it; both literals match today, so nothing is broken now.

**Recommendations** (nice to have):
- **F11 (new)** — `intent.md`/`contract.md` still describe an "empty placeholder"
  secret and omit the new `TAVILY_SECRET_UNSET_SENTINEL` constant. The code is now
  ahead of its own spec; update the spec text to match.
- **F4/F5** — Both original deviations remain independently judged **acceptable**:
  the fresh-per-call Secrets client is a once-per-invocation cost with an honest
  docstring, and the literal account/region ARNs match contract.md's own pinned
  JSON under intent.md's single-account/single-region constraint.
- **F6** — Rename the shadowed `region` loop variable (`agent_runtime.py:112`).
- **F8** — Add assertions for the log/ECR ARN strings, the Secret `Name`, and the
  trust-policy token values. (F8(d), "nothing asserts Dockerfile content", is now
  **closed** by `tests/test_dockerfile.py`.)
- **F9** — Consider a non-root container user, matching the AWS template.

**Gate for the human's next step**: nothing blocks `uv run cdk deploy
AiRadarRuntimeRole` → `agentcore launch` → smoke invoke. Remember Task 3.5
(`put-secret-value`) must follow the deploy before Tavily discovery does anything —
until then the agent will correctly run RSS-only and report `tavily_enabled=False`.

## Notes for the auditor
- **Manual-only items** (not in the automated suite, verified by the human via
  the runbook): R6, R7, R11, C18, T15, and the real `cdk deploy`. Mirrors Spec
  02's live-Tavily precedent — do not mark these blocked for lack of a pytest.
- **Region drift**: C7/T8 assume the `us.` Haiku profile spans
  us-east-1/us-east-2/us-west-2. If `get-inference-profile` reports different
  members at deploy time, the construct's `haiku_regions` and T8's expected ARNs
  must be updated together.
- **The `ecr:GetAuthorizationToken` `Resource:"*"`** is the single approved
  wildcard (AWS hard constraint, approved by the coordinator); T11 asserts it is
  the *only* one — confirmed against the synthesized template (7 statements, one
  wildcard).
- **Audited 2026-07-27** against commit `66ba0fb` + the uncommitted
  runtime-packaging working tree; **re-audited the same day** after the F1/F2
  fixes. Offline verification only — per R6/R12 no real AWS call was made in
  either pass (no `cdk deploy`, no `agentcore` CLI, no Secrets Manager /
  DynamoDB / Bedrock). Tasks 3.3–3.6, 4.4, the teardown in 4.6 and T15 remain
  correctly out of this audit's scope.
- **On trusting fix reports**: the re-audit did not accept the executor's summary
  or its edit to this file. Every claim was re-derived — full test run, CDK
  re-synth, botocore service-model lookup for the `SecretString` minimum length,
  and mutation testing of all five new/rewritten tests (4 mutants killed; 1
  drift mutant survived → Finding F10). Future fix passes should expect the same.
- **Sentinel invariant**: `infra/lib/agent_runtime.TAVILY_SECRET_UNSET_SENTINEL`
  and `curation.config.TAVILY_SECRET_UNSET_SENTINEL` must stay byte-identical.
  This is currently enforced only by comments (F10). If either changes, the
  deployed placeholder stops being recognized as unpopulated and F2 silently
  returns — with a fully green test suite.
- **tasks.md checkbox state verified accurate** (re-checked after the fix pass,
  unchanged and still correct): 1.1–1.9, 2.1–2.6, 3.1, 4.1–4.3, 4.5–4.6 checked
  and substantiated; 3.2 correctly `[~]` (Dockerfile present,
  `.bedrock_agentcore.yaml` absent — F7); 3.3–3.6 and 4.4 correctly unchecked.
  One trivial staleness: Task 4.3's trailing note still reads "61 passed
  (43 pre-existing + 18 new)" — the suite is now 66. Cosmetic; the checkbox
  itself is correct.
