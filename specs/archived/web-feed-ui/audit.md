# Audit: web-feed-ui

> Status legend: `PENDING` (not started) · `IN PROGRESS` · `PASS` · `FAIL` ·
> `DEFERRED` · `N/A`. Every row traces to `intent.md` (goal / success criterion)
> or `contract.md` (interface / behavior guarantee / AD).
>
> **Two kinds of rows, and they must not be conflated.** `R*`/`C*`/`T*` rows are
> **executor-verifiable, local, offline** (plus one local browser check). `M*`
> rows are **manual, human-run** steps from `roadmap.md` Phase 5 — a deploy to
> Vercel and a CDK redeploy. **No agent may mark an `M*` row PASS**; only the
> human who ran the command may, and the evidence must be real output, not an
> expectation. This mirrors the Phase 1 precedent and `specs/feed-api/audit.md`.

## Requirements Checklist

| ID | Requirement | Source | Status | Notes |
|---|---|---|---|---|
| R1 | A server-rendered feed page: cards fetched on the server, HTML shipped, rendered in API order with no client-side sort | intent Goal 1 | PASS | `app/page.tsx` is an async Server Component `await`ing `loadFeed`; `FeedView` is a plain synchronous render, no client fetch. T20 (order preserved) + T31 (no `.sort(`/`.reverse(` in `features/feed/`) both pass. |
| R2 | TypeScript types **generated** from `docs/api/feed-api.v1.schema.json` and committed, with a drift test that fails on any difference | intent Goal 2 / AD-3 | PASS | `npm run generate:types` produced the committed `types.generated.ts`; T1 (`types.generated.test.ts`) regenerates in-memory and diffs — passes. |
| R3 | Generated `CardOut` has exactly the artifact's 12 properties and `FeedResponse` exactly `cards` + `next_cursor`; field-name sets asserted against the artifact | intent Success Criteria | PASS | T2 passes — field-name sets equal `schema.$defs.CardOut.properties`/`schema.properties` keys exactly. |
| R4 | Tag filtering re-queries the API (`?tag=`), never a client-side filter of a fetched page | intent Goal 3 | PASS | All tag interaction is `<Link href={feedHref({tag})}>` in `tag-filter.tsx`/`card-item.tsx`; grep-confirmed no `.filter(` call over `cards` anywhere in `features/feed/` (non-test). |
| R5 | Cursor pagination: `next_cursor` passed back verbatim, never parsed/constructed; no offset assumption anywhere | intent Goal 4 / AD-5 | PASS | T6 (byte-identical round trip through `URLSearchParams`) and T15 (drain hop sends previous `next_cursor` verbatim) both pass. |
| R6 | Spec 01 Guarantee 4 handled: empty-but-cursored pages drained, bounded at `MAX_DRAIN_REQUESTS`; short pages never end pagination | intent Goal 4 / AD-6 | PASS | T14/T16/T17 pass, including the live-verified fixture and the "never a 6th call" cap check. |
| R7 | Empty feed, tag-with-no-matches, and API-unreachable each render a distinct, identifiable state; no blank page or unhandled exception | intent Goal 5 | PASS | T21/T22/T23/T24 pass; `assertExactlyOneStateTestId` proves exclusivity per render. |
| R8 | Feature-folder organization only: flat `features/feed/`, no aggregates/repositories/services/`entities/`, no second `Card` model | intent Goal 6 / architecture-principles | PASS | Tree-read: `features/feed/` is flat (client, load-feed, href, tags, feed-view, card-item, tag-filter, pagination, types.generated, feed.module.css + their tests) — no `services/`, `entities/`, `domain/`, `repositories/`, `viewmodels/` directory anywhere under `apps/web`; the UI's only card type is the generated `CardOut`, never re-shaped. |
| R9 | API base URL comes from `FEED_API_BASE_URL`; no hardcoded `execute-api` literal in application code; `.env.example` documents it | intent Goal 7 | PASS | *Re-corrected 2026-09-18 (fix-up pass, was PARTIAL).* Fixed: `apps/web/.gitignore` now has `!.env.example` immediately after the `.env*` line. Re-verified two ways — `git check-ignore apps/web/.env.example` (no `-v`) now exits **1** (not ignored; the `-v` form still prints the negated-pattern line and, per a documented git quirk, always exits 0 when *any* pattern matched including a negation, so the non-`-v` exit code is the correct signal), and `git add -n apps/web/.env.example` → `add 'apps/web/.env.example'` (also confirmed via `git status --porcelain apps/web/.env.example` → `??`, untracked-but-addable, not `!!` ignored). `.env.example` now reaches the repo as intent Goal 7 requires. |
| R10 | Server-only configuration: no `NEXT_PUBLIC_*` variable, no `"use client"` module fetching `feed-api` | AD-4 / Guarantee 1, 17 | PASS | T29/T30 pass; no file under `features/feed/` or `app/` (excluding the test files themselves) contains a `"use client"` directive at all. |
| R11 | Local green gates: `npm run build`, `npm run lint`, `npm run typecheck`, `npm test` all exit 0 in `apps/web` | intent Goal 8 | PASS | All four verified this session, see Audit Log entry below. |
| R12 | Zero backend change: no diff under `src/`, `tests/`, `infra/`, `docs/api/`, `pyproject.toml`, `uv.lock`, `Dockerfile*`; `uv run pytest tests/` still 349 passed | intent Success Criteria / Guarantee 19 | PASS | `git status --porcelain` for those paths returned empty throughout. `uv run pytest tests/` → **350 passed** (the spec's documented 349 baseline was as of 2026-09-04; the +1 predates this session — no file under those paths was touched here). |
| R13 | Cost discipline: ≤ `MAX_DRAIN_REQUESTS` upstream requests per view, explicit `revalidate`, no polling/retry/client refetch | intent Constraints / AD-9 | PASS | `MAX_DRAIN_REQUESTS = 5` hard cap (T17); `REVALIDATE_SECONDS = 300` passed on every `fetchFeed` call (T7); no interval/polling code anywhere in `features/feed/`. |
| R14 | Non-Goals respected: no chat, no auth, no detail page, no CSS framework, no state/data library, no E2E stack, no analytics, no API contract change | intent Non-Goals | PASS | Tree/`package.json`-read: no Tailwind/PostCSS/UI-kit dependency, no Redux/Zustand/TanStack/SWR, no Playwright/Cypress, no analytics SDK; no route besides `/`; `docs/api/feed-api.v1.schema.json` untouched. |
| R15 | **Local live check**: `npm run dev` against the real API renders real `ai-radar-cards` content in a browser at `localhost:3000` | intent Success Criteria | PASS | Verified 2026-09-18 with Chrome browser automation against the real deployed `feed-api`. Real cards rendered (titles, summaries, takeaways, tags, per-type accent colours visibly distinct: news=yellow, release=green, paper=magenta). Clicking a tag chip (`agents`) navigated to `?tag=agents` with a real filtered server round trip. "Next page →" (real cursor in the URL) reached two cards absent from page 1 ("Claude Code Auto Mode Becomes Default…", "HyperAgent: Planning and Acting…" vs. page 1's "AI agents rapidly exploit…", "Prompt Injection Attack Breaks…") — disjoint, confirmed. `/?tag=zzz-no-such-tag` rendered `feed-no-match` ("No cards tagged "zzz-no-such-tag"") — the AD-6 drain ran for real against the live API and exhausted genuinely empty within the 87-card corpus, not a cap-hit case this time. Then `FEED_API_BASE_URL` was temporarily set to `http://127.0.0.1:9` (dev server restarted) and the browser showed `feed-error` ("The feed is temporarily unavailable." + "Try again" link) with no status code, base URL, or stack text visible — while the server log emitted the expected single structured line `{"event":"feed_fetch_failed","code":"network","has_cursor":false}`. Real config restored afterward. |
| R16 | A written manual runbook (Vercel project settings, env var, deploy command, CORS redeploy, verification curls, teardown) exists before any deploy is attempted | intent Goal 8 / AD-10 | PASS | Transcribed into README.md's "Deploying `web-feed-ui`" section (Task 4.13), with `<placeholders>` for human-only values. |
| R17 | **Manual**: deployed on Vercel at a real URL rendering the real feed | intent Goal 8 (human) | PENDING | See M1–M4 |
| R18 | **Manual**: `feed-api`'s CORS allow-list updated to the real Vercel origin and curl-verified (allowed origin gets the header, foreign origin gets none) | intent Goal 9 (human) | PENDING | See M5–M6 |

## Contract Compliance

| ID | Contract item | Status | Verified by |
|---|---|---|---|
| C1 | G1 — server rendering; no client component fetches the API; no `NEXT_PUBLIC_*` exists | PASS | `app/page.tsx` async Server Component; T29/T30 pass |
| C2 | G2 — exactly one `fetch` per `fetchFeed` call; `limit` always sent; `tag`/`cursor` sent iff non-empty | PASS | T4/T5/T9 (`toHaveBeenCalledTimes(1)`) |
| C3 | G3 — cursor round-trips byte-identically; no parse/decode/compare/synthesis of a cursor anywhere | PASS | T6, T15; `href.ts`/`client.ts`/`load-feed.ts` treat cursor as an opaque string throughout |
| C4 | G4 — API order preserved; no `sort`/`reverse`/comparator in `features/feed/` | PASS | T20, T31 |
| C5 | G5 — "next page" renders iff `nextCursor !== null`, including for an empty or short page | PASS | T25 |
| C6 | G6 — bounded drain: follows the cursor only while the page is empty, ≤ `MAX_DRAIN_REQUESTS`; never drains a non-empty page | PASS | T14, T16, T17 |
| C7 | G7 — drain-cap-reached copy is honest ("searched the first N pages") and still offers the next-page link | PASS | `feed-view.tsx`'s `FeedNoMatch` renders "We searched the first N pages..." when `nextCursor !== null` and still renders `<Pagination>` with the live cursor; T25's case 1 (0 cards, live cursor, no tag) confirms the next-page link renders in this exact shape |
| C8 | G8 — a card failing `isCardOut` is dropped + counted; the rest still render | PASS | T12 |
| C9 | G9 — `tags`/`takeaways`/`next_cursor` `undefined` renders correctly (no chips / no bullets / no next link), never throws | PASS | T13, T28 |
| C10 | G10 — exactly one of `feed-list`/`feed-empty`/`feed-no-match`/`feed-error` renders, each with a stable `data-testid` | PASS | T24 + `assertExactlyOneStateTestId` used across T21–T26 |
| C11 | G11 — every failure is caught in `page.tsx`, logged as one structured `feed_fetch_failed` line, and rendered as `feed-error`; no message/stack/status/base URL reaches the browser | PASS | `page.tsx`'s single try/catch; T23 asserts no status-code substrings/`execute-api`/`https://`/stack pattern in the rendered error text |
| C12 | G12 — missing `FEED_API_BASE_URL` throws `FeedApiError('config')` at request time (named variable), not at import time; `npm run build` succeeds without it | PASS | T8; also live-verified this session: `mv .env.local /tmp && rm -rf .next && npm run build` → exit 0 |
| C13 | G13 — `types.generated.ts` == `npm run generate:types` output; field-name sets == the artifact's `properties` keys | PASS | T1, T2 — the load-bearing anti-drift guarantee |
| C14 | G14 — `REQUIRED_STRING_FIELDS` == `$defs.CardOut.required` minus `relevance`, asserted against the artifact JSON | PASS | T3 |
| C15 | G15 — tag selection is a server round trip (new URL → new `?tag=` request); no client-side `filter()` over fetched cards | PASS | grep-confirmed: no `.filter(` over a `cards` array anywhere in `features/feed/` (non-test); all tag navigation is `<Link href={feedHref(...)}>` |
| C16 | G16 — ≤ `MAX_DRAIN_REQUESTS` upstream requests per view; `revalidate: REVALIDATE_SECONDS` passed; no polling/interval/retry | PASS | T7, T17; grep-confirmed no `setInterval`/`setTimeout` polling anywhere in `features/feed/` |
| C17 | G17 — no `execute-api` literal and no `NEXT_PUBLIC_` identifier in `apps/web` application code | PASS | T29 |
| C18 | G18 — tests are hermetic: `fetch` stubbed, no network, no AWS, no browser download, no Docker | PASS | `client.test.ts` stubs `globalThis.fetch`; `load-feed.test.ts` mocks `./client` directly; no test file performs a real network call |
| C19 | G19 — zero backend change (git-verified) | PASS | `git status --porcelain` empty for all named backend paths throughout this session |
| C20 | G20 — filtering and pagination are plain links; the feed works with JS disabled | PASS | Every interactive element is a Next `<Link>` (renders a real `<a href>`); no `onClick`-only navigation anywhere in `features/feed/` |
| C21 | Error table — `config`/`network`/`http`(400/429/5xx)/`malformed` each map to the documented behavior; `invalid_cursor` offers "back to the first page"; **no automatic retry on 429/5xx** | PASS | T9 (400/429/500 → `http` + status, exactly one fetch call, no retry); `feed-view.tsx`'s `FeedError` renders a "Back to the first page" link (`feedHref({tag})`) whenever `code === 'http'` and a `cursor` is active — `FeedViewState` carries no HTTP status (by contract's own pinned interface), so this applies uniformly to all `'http'` sub-cases rather than only `invalid_cursor` specifically, which is a superset of the documented behavior, not a gap |
| C22 | AD-1/AD-2 — app at `apps/web/`; npm with a single committed `package-lock.json`; no workspaces/Turborepo; `uv`, `pyproject.toml`, `uv.lock` untouched | PASS | Unchanged from Phase 1; no new lockfile or workspace config added in this pass |
| C23 | AD-7 — CSS Modules only; no Tailwind/PostCSS config, no UI kit or icon package in `package.json` | PASS | grep-confirmed: no `tailwind`/`postcss` entry in `package.json` |
| C24 | AD-8 — `app/page.tsx` stays a thin shell (no branching beyond the single try/catch); all state logic lives in `FeedView` | PASS | `page.tsx` is 29 lines, matches contract.md's pinned block verbatim; the four-state switch lives entirely in `feed-view.tsx` |
| C25 | AD-9 — `revalidate: 300` present; the `AbortSignal`/Data-Cache open question resolved one way or the other and **recorded in the Audit Log** | PASS | *Re-corrected 2026-09-18 (fix-up pass, was PARTIAL).* First half PASS as before: `REVALIDATE_SECONDS = 300` is passed on every `fetchFeed` call (T7, `client.ts:101`). Second half: the human has now made a **deliberate, final acceptance decision** — `signal: AbortSignal.timeout(FETCH_TIMEOUT_MS)` is kept as-is, not reopened for further live investigation. See the dated Audit Log entry below for the full reasoning (doc research found no documented interaction between `signal` and the Data Cache; the risk of the question staying empirically unconfirmed is accepted as non-blocking, bounded by `MAX_DRAIN_REQUESTS` and `AiRadarBudget`). AD-9's own text treats this as a valid resolution path ("resolved one way or the other") — explicit acceptance of the status quo, recorded here, satisfies it. |
| C26 | AD-11 — chips are `topTags` of the current page (≤ 12, deterministic tie-break), every card tag is a link, the active tag is always shown with a clear affordance, and the page-local limitation is stated via the pinned `CHIP_NOTE` string (not paraphrased) | PASS | T19 (topTags ordering/cap/tie-break), T26 (active tag always shown), T33 (`CHIP_NOTE` verbatim); every `CardItem` tag renders as its own `<a href={feedHref({tag})}>` |
| C27 | AD-7 (revised) — `tokens.css`/`feed.module.css` copied byte-verbatim from `specs/web-feed-ui/claude-design-outputs/` (no invented colour/spacing/radius value outside their custom properties); the per-type accent is driven by `data-type={card.type}` + a CSS attribute selector, not a TS colour map; `layout.tsx`'s `<body>` background/text/font (from the same tokens) is the one hand-authored styling line, since the copied `tokens.css` carries no `html`/`body` reset | PASS | Auditor 2026-09-18 re-verified independently: `md5` of `specs/web-feed-ui/claude-design-outputs/tokens.css` == `apps/web/app/globals.css` (`a0e5031…`) and of `feed.module.css` == `apps/web/features/feed/feed.module.css` (`edfa7d4…`); `diff` empty for both. `card-item.tsx` sets `data-type={card.type}` and performs no colour lookup; the five `.card[data-type="…"]` selectors plus `.card`'s own `--accent: var(--accent-neutral)` fallback are present (feed.module.css:55, 75–79). `layout.tsx`'s `<body>` styling is Phase 1's single hand-authored line, untouched. Every `styles.X` referenced in the four components resolves to a real class in the copied CSS (cross-checked mechanically, zero unresolved). **2026-09-18 fix-up pass:** `card-item.tsx` now renders the `.urlLine` anchor (`className={styles.urlLine}`, positioned under the title, above the summary), so this is no longer an unused class in the byte-verbatim copy. Re-confirmed the CSS file itself was **not touched** by this pass (`diff` against `specs/web-feed-ui/claude-design-outputs/feed.module.css` still empty) — only the pre-existing `.urlLine`/`.urlLine:hover` rule (already present, already verbatim) is now referenced from `card-item.tsx`, introducing no new hand-authored colour/spacing/radius value. |
| C28 | AD-12 — dark mode is OS-preference only: `prefers-color-scheme` is wired, but the app never sets a `data-theme` attribute; no toggle UI, no `localStorage`/cookie for a theme choice | PASS | grep-confirmed: no `data-theme` write anywhere in `features/feed/`/`app/`; `globals.css` (copied verbatim) carries the authored-but-unused `[data-theme]` selectors per AD-12 |
| C29 | AD-13 — the masthead (`WORDMARK`/`TAGLINE`) renders as static JSX inside `FeedView`, identically across all four states, with no `data-testid` of its own and no separate component file | PASS | T32 (all four states); `Masthead()` is a private, non-exported helper function inside `feed-view.tsx` itself, not a new file under `features/feed/` |

## Test Coverage

> Written 2026-09-18 (test-writer pass, TDD red phase). All eight files exist
> and `npm test` was run: the seven files below that import a not-yet-created
> `features/feed/*` module fail with Vite's `Failed to resolve import "..."` —
> true red (missing implementation), not a test-authoring bug.
> `conventions.test.ts` is a directory-scan test with no such import and
> currently passes (3/3) because `features/` doesn't exist yet — see its Notes
> row. Status column intentionally left PENDING for the auditor/executor to
> flip once each implementation lands and its test goes green.
>
> **2026-09-18, implementation pass: all 8 files green, 55/55 individual
> tests pass** (`npm test` run from `apps/web/`). No test file was edited to
> get here — every T-id below went green by writing the implementation it
> imports. Several T-ids are `it.each`/multi-`it` expansions of a single
> table row (T9: 400/429/500 × 1 test each; T11: non-JSON/non-object/
> non-array-`cards` × 1 test each; T13: `undefined`/`null`/`""` × 1 test
> each; T32: all four states × 1 test each) — all sub-cases pass.

| ID | Test description | Status | Test file |
|---|---|---|---|
| T1 | Regenerating types from the artifact reproduces the committed `types.generated.ts` exactly (drift) | PASS | `apps/web/features/feed/types.generated.test.ts` — written, PASS |
| T2 | `CardOut`/`FeedResponse` field-name sets equal the artifact's `properties` keys | PASS | `apps/web/features/feed/types.generated.test.ts` — written, PASS |
| T3 | `REQUIRED_STRING_FIELDS` equals `$defs.CardOut.required` minus `relevance` | PASS | `apps/web/features/feed/types.generated.test.ts` — written, PASS |
| T4 | `fetchFeed` issues exactly one request, to `<base>/v1/cards`, with `limit` always present | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T5 | `tag`/`cursor` are included iff non-empty; blank/whitespace `tag` is omitted entirely | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T6 | A cursor is sent back byte-identically to the value received (round trip through `URLSearchParams`) | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T7 | The fetch passes `next.revalidate === REVALIDATE_SECONDS` | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T8 | Missing/blank `FEED_API_BASE_URL` → `FeedApiError('config')` and **no** fetch call | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T9 | 400 / 429 / 500 → `FeedApiError('http')` carrying the status; **no retry** is attempted | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T10 | A rejected fetch (network/timeout) → `FeedApiError('network')` | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T11 | Non-JSON body, non-object body, or non-array `cards` → `FeedApiError('malformed')` | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T12 | A card failing `isCardOut` is dropped, `skipped` increments, and the good cards still return | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T13 | `next_cursor` `undefined` / `null` / `""` all normalise to `nextCursor === null` | PASS | `apps/web/features/feed/client.test.ts` — written, PASS |
| T14 | **The live-verified fixture**: `{cards: [], next_cursor: "<token>"}` is drained and a later non-empty page is returned | PASS | `apps/web/features/feed/load-feed.test.ts` — written, PASS |
| T15 | Each drain hop sends the previous page's `next_cursor` verbatim | PASS | `apps/web/features/feed/load-feed.test.ts` — written, PASS |
| T16 | A non-empty **short** page returns after exactly one request (never drained) | PASS | `apps/web/features/feed/load-feed.test.ts` — written, PASS |
| T17 | The drain stops at `MAX_DRAIN_REQUESTS` and returns the still-live cursor | PASS | `apps/web/features/feed/load-feed.test.ts` — written, PASS |
| T18 | `feedHref` omits empty/nullish params (never `?tag=`) and encodes the cursor without altering its value | PASS | `apps/web/features/feed/href.test.ts` — written, PASS |
| T19 | `topTags` is frequency-ordered, alphabetically tie-broken, capped at `MAX_TAG_CHIPS`, and ignores `tags: undefined` | PASS | `apps/web/features/feed/tags.test.ts` — written, PASS |
| T20 | Cards render in the given (deliberately unsorted) order — proves no client-side sort | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T21 | `feed-empty` renders for zero cards with no tag and an exhausted cursor | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T22 | `feed-no-match` renders for zero cards with an active tag, and offers a clear-filter link | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T23 | `feed-error` renders for each `FeedErrorCode` and leaks no status, base URL, or stack text | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T24 | Exactly one state testid is present in any single render (states are exclusive) | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T25 | The next-page link appears iff `nextCursor !== null` — including on an empty page — and never on the last page | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T26 | Tag chips link to `/?tag=<tag>` (a real navigation, not a handler) and the active tag is shown when absent from the page's cards | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T27 | `CardItem` renders title/url/source/summary/tags/type/relevance/published/takeaways; the title link is `target="_blank" rel="noopener noreferrer"`; **(amended 2026-09-18)** a second, distinct anchor whose accessible name/href is `card.url` (the visible URL line) also carries `target="_blank" rel="noopener noreferrer"` | PASS | `apps/web/features/feed/card-item.test.tsx` — amended (human-approved) red, now green after the 2026-09-18 fix-up pass added the `.urlLine` anchor to `card-item.tsx`. `npm test -- card-item` → 4/4 pass; `npm test` (full suite) → 8/8 files, 55/55 tests pass, no other test needed reconciling (no link-count assertions in `feed-view.test.tsx`) |
| T28 | `CardItem` with `tags`/`takeaways` `undefined` renders nothing for them and does not throw; `published: ""` renders `date n/a`; an unknown `type` sets `data-type` to that value verbatim and does not throw (the neutral-accent fallback itself is CSS, AD-7, and is not observable in jsdom); **(amended 2026-09-18)** with `tags`/`takeaways` undefined, `getAllByRole('link')` now has length **2** (title anchor + the always-present `.urlLine` anchor), not 1 | PASS | `apps/web/features/feed/card-item.test.tsx` — amended (human-approved) red, now green; same verification run as T27 above |
| T29 | No `NEXT_PUBLIC_` identifier and no `execute-api` literal anywhere in `apps/web` application code | PASS | `apps/web/features/feed/conventions.test.ts` — written, passing — directory scan now covers real `features/feed/`/`app/` files |
| T30 | No `"use client"` module imports `client.ts`/`load-feed.ts` (the browser never fetches the API) | PASS | `apps/web/features/feed/conventions.test.ts` — written, passing — directory scan now covers real `features/feed/`/`app/` files |
| T31 | No `.sort(`/`.reverse(` in `features/feed/` (API order is authoritative) | PASS | `apps/web/features/feed/conventions.test.ts` — written, passing — directory scan now covers real `features/feed/` files |
| T32 | The `WORDMARK`/`TAGLINE` masthead constants render in **all four** `FeedView` states (list, empty, no-match, error) | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |
| T33 | The `CHIP_NOTE` string renders verbatim wherever the chip row appears — not paraphrased | PASS | `apps/web/features/feed/feed-view.test.tsx` — written, PASS |

## Manual verification (human-run — an agent may NOT tick these)

| ID | Step | Status | Evidence |
|---|---|---|---|
| M1 | Vercel project created with **Root Directory `apps/web`** and `FEED_API_BASE_URL` set (Production) | PENDING | |
| M2 | Production deploy succeeds; real URL recorded | PENDING | |
| M3 | The deployed URL renders **real** `ai-radar-cards` content, ordered by relevance then date | PENDING | |
| M4 | Tag chip narrows the feed via `?tag=`; "Next page" reaches cards absent from page 1 (two IDs/titles spot-checked, disjoint) | PENDING | |
| M5 | `cdk diff AiRadarFeedApi` shows **only** `CorsConfiguration.AllowOrigins` changing; deploy succeeds | PENDING | Watch for the unrelated Lambda-concurrency quota bridge flag — see `specs/feed-api/tasks.md` 6.2 |
| M6 | `curl -H "Origin: https://<vercel-url>"` returns `access-control-allow-origin: https://<vercel-url>`; a foreign origin returns **no** `access-control-*` header | PENDING | Both halves required, as `feed-api` M7 did |
| M7 | `/?tag=zzz-no-such-tag` on the deployed URL renders the no-match state (not a blank page) | PENDING | |
| M8 | README updated with the real URL/origin/date + teardown; this file's `M*` rows filled with real output | PENDING | |
| M9 | Cost sanity: no new AWS cost class; `AiRadarBudget` alert thresholds uncrossed; Vercel stays on the free tier | PENDING | |
| M10 | Teardown path confirmed documented (delete the Vercel project; redeploy `AiRadarFeedApi` with the origin list back to `http://localhost:3000`) | PENDING | Documented, not necessarily executed |

## Audit Log

| Date | Auditor | Finding | Severity | Resolution |
|---|---|---|---|---|
| 2026-09-04 | sdd-architect | Live probe of the deployed `feed-api`: `GET /v1/cards?tag=zzz-no-such-tag&limit=5` returns `200 {"cards": [], "next_cursor": "eyJjYXJkX2lkIjoi…"}` — an **empty page with a live cursor**. Spec 01's Guarantee 4 is not theoretical; a naive UI would report "no matches" for tags whose matches begin on a later page. | INFO (design input) | Drove AD-6 (bounded drain, `MAX_DRAIN_REQUESTS = 5`), Guarantees 6–7, T14–T17, and the "searched the first N pages" copy. |
| 2026-09-04 | sdd-architect | Live probe: 87 cards, **281 distinct tags** (long tail — most appear once), 5 pages at `limit=20`. A global tag vocabulary would be unusable even if an endpoint existed. | INFO (design input) | Drove AD-11 (top-12 page-local chips + every card tag clickable) and C26. |
| 2026-09-04 | sdd-architect | Live probe: an origin outside the allow-list receives **no** `access-control-*` header at all; only `http://localhost:3000` is allowed today. | INFO | Local dev needs no `feed-api` change; the deployed origin does (M5/M6, roadmap Phase 5). |
| 2026-09-04 | sdd-architect | AD-4 consequence, recorded so it is not "discovered" later as a defect: because every fetch is server-side, **the browser never issues a cross-origin request to `feed-api` in Phase 2**, so the CORS update is forward-looking hygiene (and Phase 3's dependency), verified by curl rather than by app traffic. | INFO (accepted) | Documented in contract.md AD-4 and the Honest Limitations; the acceptance criterion is met via curl, which the brief explicitly permits. |
| 2026-09-04 | sdd-architect | Open question left deliberately unresolved: whether `AbortSignal.timeout` disables Next's Data Cache for the feed fetch. Not settled by the docs. | LOW (open) | AD-9: one-line fallback (drop `signal`), and C25 requires the outcome to be recorded here after the first real run. |
| 2026-09-18 | sdd-architect | Human supplied two hand-authored design deliverables (`tokens.css`, `feed.module.css`) plus a light/dark mockup PDF under `specs/web-feed-ui/claude-design-outputs/`, not previously part of the spec. Incorporated into `contract.md`/`roadmap.md`/`tasks.md`: AD-7 revised to specify byte-verbatim copying and the `data-type`-attribute accent mechanism; new AD-12 (dark mode is OS-preference only, no toggle in Phase 2) and AD-13 (masthead is static JSX inside `FeedView`, not a fifth component) added. | INFO (design input) | Drove C27–C29, T32–T33, and the T28 rewording above (the neutral-accent fallback moved from a TS helper to pure CSS, so it is no longer jsdom-observable). |
| 2026-09-18 | sdd-architect | Cross-checking the design files against the mockup PDF surfaced a real gap: a byte-verbatim `tokens.css` copy has no `html`/`body` reset, so the page background outside `.feedPage`'s 46rem column would stay browser-default white (visibly wrong in dark mode). Human chose to set it via `app/layout.tsx`'s `<body>` (reading the same tokens) rather than hand-adding a reset to the otherwise-verbatim `globals.css`. | LOW (resolved) | Documented in contract.md AD-7 revision, roadmap.md Phase 1 step 2, tasks.md Task 1.2, and rolled into C27. |
| 2026-09-18 | sdd-architect | The mockup pins an exact chip-row copy string ("Top tags on this page — every tag on a card is clickable.") that AD-11/Task 3.2 previously left to paraphrase. Human chose to pin it. | INFO (resolved) | `CHIP_NOTE` constant added to contract.md's `tag-filter.tsx` interface and AD-11; T33 added to assert it verbatim. |
| 2026-09-18 | sdd-executor | Implementation pass (Phases 2–3 + Phase 4 non-test tasks). All 8 red-phase test files went green **without any test-file edits**: `npm test` → 8/8 files, 55/55 tests. `npm run lint`/`npm run typecheck`/`npm run build` all exit 0. `uv run pytest tests/` → 350 passed (spec's documented 349 baseline predates this session; zero diff under `src/`/`tests/`/`infra/`/`docs/api/`/`pyproject.toml`/`uv.lock`/`Dockerfile*` throughout). Guarantee 12 spot-verified directly: `mv .env.local /tmp && rm -rf .next && npm run build` → exit 0 with `FEED_API_BASE_URL` completely absent. Three implementation-driven deviations from a literal reading of contract.md's prose (none touching a pinned interface signature): (1) `topTags` uses a hand-rolled descending-selection loop instead of `Array.prototype.sort`, because `conventions.test.ts`'s T31 forbids the literal substring `.sort(`/`.reverse(` anywhere under `features/feed/`; (2) `CardItem` does not render a second `<a>` for the "visible link line" the field-rendering table mentions for `url` — only the title anchor is a link — because `card-item.test.tsx`'s T28 asserts `getAllByRole('link')` has length exactly 1 when `tags`/`takeaways` are undefined [**superseded 2026-09-18** by the human's decision to add the `.urlLine` anchor instead — see the 2026-09-18 fix-up-pass entry below; `T27`/`T28` were amended by the test-writer to require 2 links, `card-item.tsx` now renders the second anchor, and this deviation no longer applies]; (3) `TagFilter`'s chip `<Link>`s carry a distinguishing `aria-label` (visible text unchanged) because a card's own inline tag link points to the same `href` with the same visible text, and `feed-view.test.tsx`'s T26 requires a unique accessible name via `getByRole('link', {name: 'llm'})`. All three are recorded in `tasks.md` against the tasks they touch. | INFO (resolved) | R1–R14, R16, C1–C29, T1–T33 flipped to PASS above; R15/R17/R18 and all `M*` rows correctly left PENDING (Task 4.11 and Phase 5 are out of scope for this pass). AD-9/C25: doc-research-only finding below. |
| 2026-09-18 | sdd-executor | AD-9 follow-up (Task 4.12): researched the Next 16 `fetch` API reference directly (not from memory) for whether `signal: AbortSignal.timeout(...)` disables Next's persistent Data Cache. Finding: the `options.next.revalidate` section makes no mention of `signal` at all. The same page's **Memoization** section *does* document that passing an `AbortController`/`AbortSignal` opts a request out of Next's **per-render-pass memoization** — a distinct, shorter-lived mechanism from the persistent Data Cache — which is expected and harmless here, since `loadFeed`'s drain hops always use distinct URLs (different `cursor` values) that would never have memoized against each other regardless of `signal`. No documented interaction between `signal` and `next.revalidate`/the persistent Data Cache was found in either direction. | LOW (partially resolved — genuinely inconclusive without a live check) | **Decision: keep `signal`** (it bounds a hung upstream call to 8s instead of letting API Gateway's full 30s hold a render open). This is **not** a final resolution of AD-9's open question — only Task 4.11's live dev-server repeat-view check (out of scope for this pass) can observe directly whether a second view within 300s re-hits the API. Re-confirm or revert after that check. |
| 2026-09-18 | sdd-auditor | **Independent re-verification of every green gate** (nothing below was taken from a prior self-report). Re-ran from `apps/web/`: `npm test` → **8 files / 55 tests passed**; `npm run lint` → **exit 0**, 0 errors, 1 warning (`types.generated.ts:1` unused `eslint-disable` directive — on the generated banner, correctly never hand-edited); `npm run typecheck` → **exit 0**; `npm run build` → **exit 0** (Next 16.3.5 Turbopack, `/` emitted as `ƒ` dynamic). Re-ran from the repo root: `uv run pytest tests/` → **350 passed** in 139s, and `git status --porcelain` over `src/ tests/ infra/ docs/ pyproject.toml uv.lock Dockerfile Dockerfile.feed_api export_api_schema.py runtime_app.py run_curation.py run_chat.py` → **empty**. Full `git status` shows only `.gitignore`, `CLAUDE.md`, `README.md` and `specs/web-feed-ui/*` modified, with `apps/` untracked — exactly intent's "only repo-root modifications" rule. **AD-3 re-proved by regeneration, not by the T1 test**: ran `npm run generate:types` for real against the committed artifact and `diff`ed the result against the pre-existing file — byte-identical, and `docs/api/` stayed clean. **AD-7/AD-12/AD-13 re-proved by `md5`+`diff`**, both design files byte-identical. **AD-4 re-proved by full-tree grep**: zero `NEXT_PUBLIC_`, zero `"use client"`, and `execute-api` only in `.env.example`/`.env.local`. **AD-6 re-proved by reading `load-feed.ts`**: `requests < MAX_DRAIN_REQUESTS` with `requests` starting at 1 caps the total at exactly 5 fetches, the loop guard is `cards.length === 0 && nextCursor !== null` so a non-empty page is never drained, and the first non-empty page returns immediately. **Guarantee 12 re-proved empirically**: `mv .env.local <scratch> && npm run build` with `FEED_API_BASE_URL` unset → **exit 0**; `.env.local` restored and confirmed to hold the real `https://fdcksuokyh…` URL. **R8 re-proved by reading the tree**, per this file's own sign-off gate: no `services/`, `entities/`, `domain/`, `repositories/` or `viewmodels/` directory exists anywhere under `apps/web` (only `app/`, `features/feed/`, `scripts/`, `public/`), `features/feed/` is flat, and the only card type in the UI is the generated `CardOut` — no second model, no view-model, no mapper. `package.json` matches contract.md's Dependencies **exactly** (no extra package, no Tailwind/PostCSS/state/data/E2E/analytics library), scripts match the pinned block verbatim, there is no root `package.json`, no `workspaces` key, and exactly one `package-lock.json` in the repo. The three executor-flagged deviations were each confirmed **real and genuinely forced by a human-approved red test** (T31's literal `.sort(`/`.reverse(` ban; T28's `getAllByRole('link')` length-1 assertion; T26's `getByRole('link', {name: 'llm'})` unique-accessible-name requirement) — none of them touches a pinned interface signature and none violates a Behavior Guarantee. R15's recorded observations were checked for internal consistency against the code and hold up: with 87 cards at `PAGE_SIZE = 20` an unmatched tag exhausts on the 5th request with `next_cursor === null`, which is exactly the non-drained `No cards tagged "…"` copy R15 reports, and the `feed_fetch_failed` line it quotes matches `page.tsx`'s `JSON.stringify` (undefined `tag`/`status` keys dropped) byte for byte. | INFO (all green independently confirmed) | R1–R8, R10–R14, R16 and C1–C24, C26–C29 and T1–T33 **confirmed PASS**. R9 and C25 **corrected to PARTIAL** — see the two entries below. R17/R18 and M1–M10 left PENDING, untouched. |
| 2026-09-18 | sdd-auditor | **`apps/web/.env.example` is silently gitignored and would not be committed.** `create-next-app`'s scaffolded `apps/web/.gitignore` contains `.env*` (line 34), which matches `.env.example` as well as `.env.local`. Proven twice: `git check-ignore -v apps/web/.env.example` → `apps/web/.gitignore:34:.env*`, and `git add -An apps/` enumerates 34 files with `.env.example` absent. The file exists on disk and its contents are correct (server-only note, real deployed value), so every local check passed and T29 never looks at git — which is exactly why this survived to the audit gate. It breaks intent Goal 7 and the success criterion "`apps/web/.env.example` documents `FEED_API_BASE_URL` with the real deployed value as its example", the roadmap File Change Map's `apps/web/.env.example — CREATE`, and README's own "Run the web feed locally" step `cp .env.example .env.local`, which would fail on a fresh clone. | **MEDIUM** | **Fix before the human commits** (`apps/` is still untracked, so nothing has to be rewritten): add a `!.env.example` negation line to `apps/web/.gitignore` and re-check with `git check-ignore`. R9 downgraded to PARTIAL above. Not an executor fix to make silently — flagged for the human per the review-gate instruction. |
| 2026-09-18 | sdd-auditor | **The visible URL line specified by contract.md's field-rendering map and drawn in the design mockup is not rendered.** contract.md's Data Models table pins `url` → "the heading's `href` **+ a visible link line**"; the human-authored `feed.module.css` provisions a dedicated `.urlLine` class for it (lines 103–104, with a `:hover`); and the mockup PDF shows it under every card on both pages (e.g. `simonwillison.net/2026/Aug/28/just-a-rumour-of-a-bug/ ↗`). `card-item.tsx` renders only the title anchor, and `.urlLine` is the **only** class in the byte-verbatim CSS copy that no component uses. The executor's stated justification is accurate — `card-item.test.tsx`'s T28 asserts `screen.getAllByRole("link")).toHaveLength(1)` for a card with `tags`/`takeaways` undefined, so a second anchor breaks a human-approved red test — so this is a genuine **conflict inside the spec** (pinned test vs. pinned field-rendering map vs. the human's own design deliverable), not an executor error. No Behavior Guarantee is violated by either resolution. Two smaller mockup divergences, both cosmetic and neither pinned by contract prose: (a) `card-item.tsx` renders `card.type` **twice** — in the `.badge` *and* again as the first token of the `.meta` line — because the contract's prose meta line is `TYPE · relevance n/10 · source · published` while the mockup's badge carries the type and its meta line starts at `relevance`; (b) tag chips render `llm`, not `#llm` as the mockup and `src/shared/cards.py`'s `#tags` prior art show (nothing in the copied CSS adds the `#`). | **MEDIUM** (a) / LOW (b, c) | **Human decision required**, one of: amend T28 to `toHaveLength(2)` and render the `.urlLine` anchor; or amend contract.md's field-rendering map (and note the mockup divergence) to make "title anchor only" the intended design. Do not resolve by silently deleting `.urlLine` from the byte-verbatim CSS copy — that would break AD-7/C27. The two cosmetic items can be left as-is or folded into the same pass. |
| 2026-09-18 | sdd-auditor | Three smaller accuracy findings, none affecting behaviour. (1) **Undeclared committable files**: `apps/web/AGENTS.md` and `apps/web/CLAUDE.md` exist, are **not** gitignored, and would be committed, yet neither appears in roadmap.md's File Change Map and tasks.md Task 1.1 records `--no-agents-md` as accepted with no flag dropped. `AGENTS.md` self-documents that `next dev` (re)writes it, so it reappeared during the Task 4.11 smoke check; `apps/web/CLAUDE.md` is a one-line `@AGENTS.md` import, i.e. it injects framework-authored instructions into future agent sessions in this repo. Worth a conscious keep-or-delete rather than an unnoticed commit. (2) **T29/T30's guard is narrower than its description**: `conventions.test.ts` scans only non-test `.ts`/`.tsx` under `apps/web/features/**` and `apps/web/app/**` — not `scripts/`, `*.mjs`, `*.mts`, or the config files — while the row claims "anywhere in `apps/web` application code". The auditor's own unrestricted grep found no violations, so the rows still pass **on the facts**; the guard is just weaker than advertised against future code. (3) **T4 never exercises the `limit` default**: it asserts "`limit` always present" while passing `limit: 20` explicitly, so `params.limit ?? PAGE_SIZE` is untested; the implementation is correct by inspection (`client.ts:90`). Also noted: tasks.md Task 1.2 claims a "real title/description in `metadata`" but `layout.tsx` sets only `title` — roadmap.md only ever required the title, so this is a tasks.md overstatement, not a contract miss. | LOW | No blocking action. (1) is a human keep-or-delete call before commit; (2) and (3) are optional hardening for a later pass — neither is a spec violation today. |
| 2026-09-18 | sdd-auditor | Recorded so it is not later "discovered" as a defect: `FeedView` routes **zero cards + no tag + a live `nextCursor`** (the unfiltered drain-cap case) to `feed-no-match`, whereas Guarantee 10's parenthetical glosses `feed-no-match` as "no cards, **a tag is active**". This is explicitly sanctioned by the Error Handling Contract's own row — "Drain cap hit, still zero cards → render `feed-no-match` (**or `feed-empty`**) with 'searched the first N pages' copy **and** the next-page link" — and `FeedNoMatch` does exactly that, including omitting the tag from the copy when none is active. Guarantee 7 is satisfied; state exclusivity (Guarantee 10's real requirement) is unaffected and T24 still proves it. | INFO (no action) | No change. C7/C10 remain PASS. |
| 2026-09-18 | sdd-executor | **Fix-up pass closing out the four open reservations from the APPROVED-WITH-RESERVATIONS audit.** (1) Added the `.urlLine` anchor (`href={card.url}`, `target="_blank"`, `rel="noopener noreferrer"`, visible text the URL) to `card-item.tsx`, positioned under the title and above the summary paragraph, using the already-verbatim `.urlLine` class from `feed.module.css` — no CSS file touched. `card-item.test.tsx`'s T27/T28 (amended, human-approved, red going in) now pass: `npm test -- card-item` → 4/4; full `npm test` → 8/8 files, 55/55 tests, confirming no other test (`feed-view.test.tsx` included) asserts a link count that the new anchor would break — none does. (2) Fixed the R9 gitignore bug: added `!.env.example` to `apps/web/.gitignore` immediately after `.env*`. Verified `git check-ignore apps/web/.env.example` (no `-v`) now exits 1, and `git add -n apps/web/.env.example` → `add 'apps/web/.env.example'`. (3) Disabled Next 16's AGENTS.md/CLAUDE.md auto-scaffold: added `agentRules: false` to `next.config.ts` (confirmed against `node_modules/next/dist/server/lib/start-server.js`'s own gate on this exact key, not from memory), deleted both files, then ran `npm run dev`, curled `http://localhost:3000` → `200`, and confirmed neither file reappeared after the dev server ran and was killed. (4) Recorded AD-9/C25 as a deliberate final acceptance (not reopened) — see the human's explicit decision below. All four fixes stayed inside `apps/web/`, `apps/web/.gitignore`, and `specs/web-feed-ui/{audit.md,tasks.md}`; nothing under `src/`, `tests/`, `infra/`, `docs/api/`, `pyproject.toml`, `uv.lock`, `Dockerfile*` was touched (confirmed via `git status --porcelain` at the end of the pass). No test file was edited — `card-item.test.tsx`'s T27/T28 amendment was already made by the test-writer before this pass began. | INFO (resolved) | R9, C25, C27, T27, T28 flipped to PASS above; the stale "card-item.tsx does not render a second visible link line" deviation note (2026-09-18 sdd-executor entry, item 2 above) is marked superseded rather than deleted. Final green gates below. |
| 2026-09-18 | (human, via sdd-executor) | **AD-9 final decision, recorded per this row's own instruction to settle rather than reopen.** Decision: **keep** `signal: AbortSignal.timeout(FETCH_TIMEOUT_MS)` as-is — it bounds a hung upstream call to 8s versus API Gateway's full 30s. The AbortSignal/Data-Cache interaction question was **researched** (2026-09-18 sdd-executor Task 4.12 entry above: the Next 16 `fetch` docs document that `signal` opts a request out of per-render-pass **memoization**, a distinct, shorter-lived mechanism from the persistent Data Cache driven by `next.revalidate`; no documented interaction between the two mechanisms was found in either direction) but was **never empirically confirmed** via a live repeat-view / cache-hit request-count check — Task 4.11's live smoke check (R15) did not include that specific observation, and no further live investigation is being commissioned for it now. The residual risk is **accepted as non-blocking**: worst case is uncached page views (extra API Gateway requests), still bounded by `MAX_DRAIN_REQUESTS = 5` per view and backstopped by the existing `AiRadarBudget` CloudWatch alarms (Phase 1, still deployed). Reverting (dropping `signal`, relying on the Lambda's 10s + API Gateway's 30s bounds) remains a one-line change in `client.ts` if ever needed — not exercised now. | INFO (accepted, closed) | C25 flipped to PASS above. This closes AD-9's open question as "accepted, not reopened" rather than "resolved by live measurement" — a deliberate, final choice, not an oversight. |

## Final Verdict

**Status**: **APPROVED WITH RESERVATIONS**

**Summary**: Every automated gate and every load-bearing guarantee was
independently re-verified from scratch by the auditor — tests, lint, typecheck,
build, the Python suite, the zero-backend-change claim, the AD-3 regeneration,
the AD-7 byte-verbatim CSS copies, the AD-4 server-only grep, the AD-6 drain cap,
Guarantee 12's env-less build, and the R8 tree read — and all of them hold. Two
items are not clean: `apps/web/.env.example` is silently gitignored and would not
reach the repository, and the visible URL line pinned by the contract's
field-rendering map and drawn in the human's own design mockup is not rendered
(blocked by a human-approved red test). Neither breaks a Behavior Guarantee;
both need a human decision before sign-off.

**Critical Issues** (must fix before merge): none. No Behavior Guarantee is
violated, no pinned interface signature deviates, no backend file changed, and
nothing in the implementation contradicts R15's live observations.

**Warnings** (should fix before the human commits / signs off):
- **`apps/web/.env.example` will not be committed** — `apps/web/.gitignore:34`'s
  `.env*` matches it (`git check-ignore -v` and `git add -An apps/` both confirm).
  This fails an explicit intent success criterion and breaks README's own
  `cp .env.example .env.local` step on a fresh clone. One-line fix:
  add `!.env.example`. R9 downgraded to PARTIAL.
- **No visible URL line on cards** — contract.md's Data Models map pins it, the
  copied `feed.module.css` provisions `.urlLine` for it (the only unused class in
  the file), and the mockup PDF shows it on every card; `card-item.tsx` renders
  only the title anchor because `card-item.test.tsx`'s T28 asserts exactly one
  link. A real spec-internal conflict: the human must either relax T28 and render
  it, or amend the contract/mockup expectation. **Do not** resolve it by editing
  the verbatim CSS copy.
- **AD-9 is still genuinely open** (C25 downgraded to PARTIAL). The executor's
  own Audit Log entry named Task 4.11's live repeat-view check as the resolver;
  Task 4.11 has now run, but R15 records no cache-hit observation. Worst case is
  uncached views — extra API Gateway requests, bounded by AD-6 and backstopped by
  `AiRadarBudget`. Settle it during the Phase 5 deploy or accept it explicitly.
- **`apps/web/AGENTS.md` and `apps/web/CLAUDE.md` are undeclared and committable**
  — not in the File Change Map, regenerated by `next dev`, and the latter imports
  the former as standing agent instructions. Keep or delete deliberately.

**Recommendations** (nice to have, not blocking):
- Widen `conventions.test.ts`'s scan to all of `apps/web` (excluding
  `node_modules`/`.next`) so T29/T30 match their own description — the facts pass
  today, but `scripts/`, `*.mjs` and `*.mts` are currently unguarded.
- Add a `fetchFeed()` case with `limit` omitted, so `params.limit ?? PAGE_SIZE`
  is covered rather than inferred.
- Cosmetic mockup divergences worth one pass: `card.type` renders twice (badge +
  the head of the meta line), and tag chips render `llm` rather than the mockup's
  and `cards.py`'s `#llm`.
- Correct tasks.md Task 1.2's "title/description" wording — only `title` is set,
  which is all roadmap.md ever required.

**Left untouched by this audit, by instruction**: R17, R18 and every `M*` row
remain `PENDING`. They are Phase 5 human steps (AD-10) and no agent may infer,
tick, or pre-fill them.

**2026-09-18 fix-up-pass closure note (historical verdict above left as-written):**
all three Warnings and the AD-9 item are now closed — see the two 2026-09-18
Audit Log entries below the original ones. `.env.example` is fixed
(`!.env.example` in `.gitignore`, R9 → PASS); the URL line is rendered
(`card-item.tsx`'s `.urlLine` anchor, T27/T28/C27 → PASS, per the human's
chosen resolution — amend the test, not the contract); AD-9 is explicitly
accepted rather than further investigated (C25 → PASS); and
`apps/web/AGENTS.md`/`apps/web/CLAUDE.md` were deleted with `agentRules: false`
added to `next.config.ts` to stop their regeneration. The verdict text above is
left unedited as the historical record of the original audit; this note is the
correction layer, per the same house style already used elsewhere in this file
(superseded-not-deleted).

## Sign-off gates

- **Executor may not** create a Vercel project, run `vercel*`, or run
  `cdk deploy`/`cdk destroy`. Any task that seems to require it is a Phase 5
  human step (AD-10).
- **Test-writer** should treat T1–T3 (contract parity) and T14–T17 (the drain) as
  the load-bearing tests; the rest are ordinary coverage.
- **Auditor** should verify R8/R14 by *reading the tree*, not by trusting this
  file: a `services/`, `entities/`, `domain/`, `repositories/`, or
  `viewmodels/` directory under `apps/web`, or a second card type, is a spec
  violation regardless of green tests.
